% ============================================================
% source.tex — Ch 11 : Fonctions numériques — Tronc Commun Maroc
% Migré depuis Contenu.tex + chapters/
% ============================================================

%% HEADER_APERCU — NE PAS EXTRAIRE
\documentclass[10pt, a4paper]{article}
\input{../../preamble/01_packages}
\input{../../preamble/02_style}
\input{../../preamble/03_macros}
\begin{document}
\pagestyle{cours}
%% FIN_HEADER_APERCU

%% SECTION
\section{Fonction numérique d'une variable réelle \duree{15 min}}

%% ACTIVITE
\begin{Activite}[][5 min]
Ahmed dispose d'un morceau de fil de fer de $20\text{ cm}$ de longueur.
Il le plie pour former un rectangle de longueur $x$ et de largeur $y$.
\begin{enumerate}
    \item Exprimer la largeur $y$ en fonction de la longueur $x$.
    \item Déterminer l'intervalle $D$ auquel doit appartenir $x$ pour que ce rectangle puisse exister.
    \item Montrer que la surface du rectangle, notée $S(x)$, s'écrit $S(x) = 10x - x^2$
    \item Calculer la surface lorsque $x = 2$, puis lorsque $x = 8$.
    \item Pour quelle(s) valeur(s) de $x$ la surface est-elle égale à $25\text{ cm}^2$ ?
\end{enumerate}
\end{Activite}

%% SOLUTION_ACTIVITE
\begin{Solution}
\begin{enumerate}
    \item \textbf{Exprimons la largeur $y$ en fonction de $x$ :}

    Le périmètre du rectangle est égal à la longueur totale du fil, soit $20\text{ cm}$.
    Le périmètre est : $2x + 2y = 20$.

    Donc : $x + y = 10$

    D'où :   $y = 10 - x$


    \item \textbf{Déterminons l'intervalle $D$ auquel doit appartenir $x$ :}

    Pour que le rectangle existe, les longueurs $x$  et $y$ doivent être strictement positives :
    \[
\begin{cases}
    x > 0 \\
    y > 0
\end{cases} \quad \text{alors} \quad
\begin{cases}
    x > 0 \\
    10 - x > 0
\end{cases} \quad \text{donc} \quad
\begin{cases}
    x > 0 \\
    x < 10
\end{cases}
\]
    Donc,  $0<x<10$ . L'intervalle est : $D = \left] 0 ; 10 \right[$


    \item \textbf{Montrons que la surface $S(x) = 10x - x^2$ :}

    La surface d'un rectangle est donnée par la formule :
    $$S = \text{longueur} \times \text{largeur}$$
    On a donc :
    $ S\left( x \right) = x \times \left( 10 - x \right) = 10x - x^2$

    \item \textbf{Calculons la surface pour $x = 2$ et $x = 8$ :}

    \begin{itemize}
        \item Pour $x = 2$ :
         $S\left( 2 \right) = 10 \times 2 - 2^2 = 20 - 4 = 16\text{ cm}^2 $
        \item Pour $x = 8$ :
        $S\left( 8 \right) = 10 \times 8 - 8^2 = 80 - 64 = 16\text{ cm}^2$
    \end{itemize}
    On remarque que deux longueurs différentes peuvent donner la même surface.

    \item \textbf{Déterminons $x$ pour que la surface soit égale à $25\text{ cm}^2$ :}

    Résolvons l'équation $S\left( x \right) = 25$ :
   \begin{align*}
    S\left( x \right) = 25 \quad \text{ équivaut à } \qquad 10x - x^2 &= 25 \\
     x^2 - 10x + 25 &= 0 \\
     \left( x - 5 \right)^2 &= 0 \\
     x - 5 &= 0 \\
     x &= 5
    \end{align*}
        La surface est de $25\text{ cm}^2$ lorsque $x = 5\text{ cm}$.
\end{enumerate}
\end{Solution}

%% DEFINITION
\begin{Definition}[Fonction numérique][]
\begin{itemize}
    \item On appelle \textbf{fonction numérique} d'une variable réelle $x$ toute relation $x \mapsto f(x)$ qui associe à chaque $x$ au plus une valeur.
    \item Si $f(x)$ existe, on dit que $f(x)$ est l'\textbf{image} de $x$.
    \item L'ensemble des nombres réels $x$ qui ont une image par $f$ est appelé \textbf{ensemble de définition} de $f$ et se note $D_f$.
\end{itemize}
\end{Definition}

%% EXEMPLE
\begin{Exemple}[][10 min]
\begin{enumerate}
    \item Soit la fonction numérique : $f(x) = \dfrac{2x}{x-1}$

    L'expression $\dfrac{2x}{x-1}$ est définie si : $x - 1 \neq 0$

    C'est-à-dire : $x \neq 1$ \\
    Donc l'ensemble de définition de $f$ est : $$D_f = ]-\infty ; 1[ \cup ]1 ; +\infty[$$ ou encore $D_f = \mathbb{R} \setminus \{1\}$

    \item Soit $g$ la fonction : $g(x) = \sqrt{2x - 3}$

    L'expression $\sqrt{2x - 3}$ est définie si $2x - 3 \geq 0$

    C'est-à-dire : $x \geq \dfrac{3}{2}$

    Donc l'ensemble de définition de $g$ est : $$D_g = \left[\dfrac{3}{2} ; +\infty\right[$$
\end{enumerate}
\end{Exemple}

%% APPLICATION
\begin{Application}[][10 min]
Déterminer l'ensemble de définition de la fonction $f$ dans chacun des cas suivants :
\begin{enumerate}
    \item $f(x) = x^2 - 2x + 1$
    \item $f(x) = \dfrac{x+1}{2x-8}$
    \item $f(x) = \sqrt{2-x}$
    \item $f(x) = \sqrt{x^2 - 5x + 6}$
    \item $f(x) = \dfrac{\sqrt{x}}{x^2 - 2x + 1}$
    \item $f(x) = \dfrac{x+4}{|x| - 3}$
    \item $f(x) = \dfrac{\sqrt{2-x}}{\sqrt{4x+2}}$
    \item $f(x) = \sqrt{\dfrac{2-x}{4x+2}}$
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}

    \item $f\left( x \right) = x^2 - 2x + 1$

    Puisque $f$ est une fonction polynôme, elle est définie sur $\mathbb{R}$ tout entier.
    \[ D_f = \mathbb{R} = \left] -\infty ; +\infty \right[ \]

    \item  $f\left( x \right) = \dfrac{x+1}{2x-8}$
    \begin{align*}
         D_f = \left\{ x \in \mathbb{R} \mid 2x - 8 \neq 0 \right\}
             = \left\{ x \in \mathbb{R} \mid 2x  \neq 8 \right\}
             = \left\{ x \in \mathbb{R} \mid x  \neq 4 \right\}
    \end{align*}
    \[ D_f = \mathbb{R} \setminus \left\{ 4 \right\} = \left] -\infty ; 4 \right[ \cup \left] 4 ; +\infty \right[ \]

    \item  $f\left( x \right) = \sqrt{2-x}$
    \begin{align*}
        D_f = \left\{ x \in \mathbb{R} \mid 2 - x \geq 0 \right\}
         = \left\{ x \in \mathbb{R} \mid  - x \geq -2 \right\}
         = \left\{ x \in \mathbb{R} \mid x \leq 2 \right\}
    \end{align*}
    \[ D_f = \left] -\infty ; 2 \right] \]

    \item $f\left( x \right) = \sqrt{x^2 - 5x + 6}$
    \[ D_f = \left\{ x \in \mathbb{R} \mid x^2 - 5x + 6 \geq 0 \right\} \]
    Étudions le signe du trinôme $x^2 - 5x + 6$

    Puisque $\Delta = 1 > 0$, le trinôme admet deux racines réelles distinctes :
    \begin{align*}
        x_1 &= \dfrac{-\left( -5 \right) - \sqrt{1}}{2 \times 1} = \dfrac{5 - 1}{2} = 2 \\
        x_2 &= \dfrac{-\left( -5 \right) + \sqrt{1}}{2 \times 1} = \dfrac{5 + 1}{2} = 3
    \end{align*}

\begin{center}
    \begin{tikzpicture}
    \tkzTabInit[lgt=2, espcl=1.6]{$x$ / 0.5 , $x^2 - 5x + 6$ / 1}{$-\infty$, $2$, $3$, $+\infty$}
    \tkzTabLine{ , +, z, -, z, +, }
\end{tikzpicture}
\end{center}

    \[ D_f = \left] -\infty ; 2 \right] \cup \left[ 3 ; +\infty \right[ \]

    \item  $f\left( x \right) = \dfrac{\sqrt{x}}{x^2 - 2x + 1}$
    \begin{align*}
        D_f &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad x^2 - 2x + 1 \neq 0  \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad (x-1)^2 \neq 0  \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad x-1 \neq 0  \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad x \neq 1  \right\}
    \end{align*}
    L'ensemble de définition est donc :
    $D_f = \left[ 0 ; 1 \right[ \cup \left] 1 ; +\infty \right[$

    \item $f\left( x \right) = \dfrac{x+4}{|x| - 3}$
    \[ D_f = \left\{ x \in \mathbb{R} \mid |x| - 3 \neq 0 \right\} \]
    \begin{align*}
        |x| - 3 = 0 &\text{ équivaut à } |x| = 3 \\
        &\text{ équivaut à } x = 3 \text{ ou } x = -3
    \end{align*}
    \[ D_f = \mathbb{R} \setminus \left\{ -3 ; 3 \right\} = \left] -\infty ; -3 \right[ \cup \left] -3 ; 3 \right[ \cup \left] 3 ; +\infty \right[ \]

    \item $f\left( x \right) = \dfrac{\sqrt{x-3}}{\sqrt{x+1}}$


        \[ D_f = \left\{ x \in \mathbb{R} \mid  x - 3 \geq 0 \quad \text{et} \quad x +   > 0 \right\} \]
    Cherchons l'intersection des deux conditions :
    \begin{align*}
        \begin{cases} x - 3 \geq 0 \\ x + 1 > 0 \end{cases} &\text{ équivaut à } \begin{cases} x \geq 3 \\ x > -1 \end{cases} ~~
        \text{ équivaut à } ~~ x \geq 3
    \end{align*}
    Donc : \qquad
     $D_f = \left[ 3 ; +\infty \right[$

    \item  $f\left( x \right) = \sqrt{\dfrac{x-3}{x+1}}$
    \[ D_f = \left\{ x \in \mathbb{R} \mid \dfrac{x-3}{x+1} \geq 0 \quad \text{et} \quad x + 1 \neq 0  \right\} \]
    \begin{itemize}
        \item $x - 3 = 0$ équivaut à $x = 3$
        \item $x + 1 = 0$ équivaut à $x = -1$
    \end{itemize}
    Dressons le tableau de signe de $\dfrac{x-3}{x+1}$:
    \vspace{0.1cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1.5, espcl=2]{$x$ / 0.6 , $x - 3$ / 0.7 , $x + 1$ / 0.7 , $\dfrac{x-3}{x+1}$ / 1}{$-\infty$, $-1$, $3$, $+\infty$}
    \tkzTabLine{ , -, t, -, z, +, }
    \tkzTabLine{ , -, z, +, t, +, }
    \tkzTabLine{ , +, d, -, z, +, }
\end{tikzpicture}
\end{center}
    \[ D_f = \left] -\infty ; -1 \right[ \cup \left[ 3 ; +\infty \right[ \]
    \textbf{Règle d'or :} Il faut toujours chercher l'ensemble de définition sur l'expression de départ, sans jamais la simplifier ni la transformer !
\end{enumerate}
\end{Solution}

%% TECHNIQUES
\begin{Techniques}[][]
Soient $P(x)$ et $Q(x)$ deux polynômes ; on a :

\vspace{0.3cm}
\begin{center}
\renewcommand{\arraystretch}{1.5}
\begin{tabular}{ScSc}
\toprule
\theadrow Fonction & Ensemble de définition \\
\midrule
$x \mapsto P(x)$ & $D_f = \mathbb{R} = ]-\infty ; +\infty[$ \\
$x \mapsto \dfrac{P(x)}{Q(x)}$ & $D_f = \{x \in \mathbb{R} / Q(x) \neq 0\}$ \\
$x \mapsto \sqrt{P(x)}$ & $D_f = \{x \in \mathbb{R} / P(x) \geq 0\}$ \\
$x \mapsto \dfrac{P(x)}{\sqrt{Q(x)}}$ & $D_f = \{x \in \mathbb{R} / Q(x) > 0\}$ \\
$x \mapsto \dfrac{\sqrt{P(x)}}{\sqrt{Q(x)}}$ & $D_f = \{x \in \mathbb{R} / P(x) \geq 0 \text{ et } Q(x) > 0\}$ \\
$x \mapsto \sqrt{\dfrac{P(x)}{Q(x)}}$ & $D_f = \left\{x \in \mathbb{R} / \dfrac{P(x)}{Q(x)} \geq 0 \text{ et } Q(x) \neq 0\right\}$ \\
\bottomrule
\end{tabular}
\end{center}
\end{Techniques}


%% SECTION
\section{Égalité de deux fonctions \duree{15 min}}

%% DEFINITION
\begin{Definition}[hi][]
Soient $f$ et $g$ deux fonctions et $D_f$ et $D_g$ leurs ensembles de définition respectifs. \\
On dit que les fonctions $f$ et $g$ sont \textbf{égales } et on écrit $f = g$ si :
     $$D_f = D_g ~\text{ et pour tout}~  x ~\text{de}~ D_f : f(x) = g(x)$$
\end{Definition}

%% EXEMPLE
\begin{Exemple}[][10 min]
Soient $f$ et $g$ deux fonctions définies par :
 $f(x) = \sqrt{x^4 + 2x^2 + 1} \quad \text{et} \quad g(x) = x^2 + 1 $

\vspace{0.2cm}
On a $D_f = D_g = \mathbb{R}$ et
$f(x) = \sqrt{(x^2 + 1)^2} = x^2 + 1 \quad (\text{car } x^2 + 1 > 0)$ \\
Donc $f(x) = g(x)$, pour tout $x$ de $\mathbb{R}$.
D'où : \quad $f = g$
\end{Exemple}

%% APPLICATION
\begin{Application}[][5 min]
\noindent
On considère les fonctions numériques $f$ et $g$ définies par :
$f(x) = \frac{1}{\sqrt{x}-2} \quad \text{et} \quad g(x) = \frac{\sqrt{x}+2}{x-4}$

Montrer que $f=g$.
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{itemize}
    \item \textbf{Déterminons $D_f$ :}
     $f\left( x \right) = \dfrac{1}{\sqrt{x}-2}$
     \begin{align*}
        D_f &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad \sqrt{x} - 2 \neq 0  \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad \sqrt{x}\neq 2  \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad \left( \sqrt{x} \right)^2 \neq 2^2  \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad x \neq 4  \right\}
    \end{align*}
     Donc : \quad $D_f = \left[ 0 ; 4 \right[ \cup \left] 4 ; +\infty \right[$

    \item \textbf{Déterminons $D_g$ :}
     $g\left( x \right) = \dfrac{\sqrt{x}+2}{x-4}$
    \begin{align*}
        D_g &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad x - 4 \neq 0 \right\} \\
         &= \left\{ x \in \mathbb{R} \mid  x \geq 0 \quad \text{et} \quad x \neq 4 \right\}
    \end{align*}
  Donc : \quad   $D_g = \left[ 0 ; 4 \right[ \cup \left] 4 ; +\infty \right[ $

   Nous constatons que les deux ensembles sont identiques :
\[ D_f = D_g = \mathbb{R}^+ \setminus \left\{ 4 \right\} = \left[ 0 ; 4 \right[ \cup \left] 4 ; +\infty \right[ \]

\item Soit $x$ de $D_f$. Calculons $f\left( x \right)$ :
\begin{align*}
    f\left( x \right) &= \dfrac{1}{\sqrt{x}-2}
    = \dfrac{\left( \sqrt{x} + 2 \right)}{\left( \sqrt{x} - 2 \right)\left( \sqrt{x} + 2 \right)} = \dfrac{\sqrt{x} + 2}{x - 4} =g(x)
\end{align*}
\textbf{Conclusion :}
Puisque $D_f = D_g$ et pour tout $x$ de $D_f$, $f\left( x \right) = g\left( x \right)$, Alors  :
    $f = g$
\end{itemize}
\end{Solution}

%% SECTION
\section{Représentation graphique d'une fonction \duree{15 min}}

%% DEFINITION
\begin{Definition}[hi][]
La \textbf{courbe représentative} de $f$ (ou \textbf{graphe} de $f$), notée $\mathcal{C}_f$, est l'ensemble des points $M(x\,;\,y)$ du plan tels que $x \in D_f$ et $y = f(x)$ :

$$\mathcal{C}_f = \bigl\{M\bigl(x\,;\,f(x)\bigr) \mid x \in D_f\bigr\}$$

\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        width=9cm, height=4.5cm,
        axis lines=middle,
        axis line style={thick, black, -stealth},
        xmin=-0.3, xmax=4.2,
        ymin=-0.1, ymax=2.3,
        xtick=\empty, ytick=\empty,
        tick style={color=black},
        clip=false,
    ]
        \addplot[
            thick, color=SecondaryColor, smooth,
            domain=0:4, samples=120
        ]{(x+0.3)*(4-x)*0.4};

        \draw[dashed, color=black!40, line width=0.8pt]
            (axis cs:2, 0) -- (axis cs:2, 1.84);
        \draw[dashed, color=black!40, line width=0.8pt]
            (axis cs:0, 1.84) -- (axis cs:2, 1.84);

        \filldraw[black] (axis cs:2, 1.84) circle (2.5pt);

        \node[above right, font=\small\sffamily, color=black]
            at (axis cs:2, 1.84) {$M(x\,;\,y)$};
        \node[left, font=\small\sffamily, color=black]
            at (axis cs:0, 1.84) {$f(x)$};
        \node[below, font=\small\sffamily, color=black]
            at (axis cs:2, 0) {$x$};
        \node[right, font=\small\sffamily\itshape, color=SecondaryColor]
            at (axis cs:3.7, 0.5) {$\mathcal{C}_f$};
    \end{axis}
\end{tikzpicture}
\end{center}

\end{Definition}

%% EXEMPLE
\begin{Exemple}[][10 min]
Soit $f$ la fonction définie sur $\mathbb{R}$ par : $f(x) = |2x - 1|$
\vspace{0.2cm}
\begin{itemize}
    \item On a $f\!\left(\frac{1}{2}\right) = 0$ donc le point $A\!\left(\frac{1}{2} ; 0\right) \in (\mathcal{C}_f)$
    \item On a $f(2) = 3$ donc le point $B(2 ; 3) \in (\mathcal{C}_f)$
\end{itemize}
\end{Exemple}

%% APPLICATION
\begin{Application}[][5 min]
\noindent
Considérons la fonction $f$ définie par $f(x) = \frac{x^2}{x+1}$

Parmi les points $A(0;0)$, $B(-1;1)$, $C\left(3;\frac{9}{4}\right)$ et $D(2;4)$

déterminer ceux qui appartiennent à $(C_f)$.
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
On a: \quad $f\left( x \right) = \dfrac{x^2}{x+1}$ avec $D_f = \mathbb{R} \setminus \left\{ -1 \right\}$

\begin{itemize}
    \item On a $f\left( 0 \right) = 0$ donc le point $A\left( 0 ; 0 \right) \in \left( \mathcal{C}_f \right)$
    \item On a $-1 \notin D_f$ donc le point $B\left( -1 ; 1 \right) \notin \left( \mathcal{C}_f \right)$
    \item On a $f\left( 3 \right) = \dfrac{9}{4}$ donc le point $C\left( 3 ; \dfrac{9}{4} \right) \in \left( \mathcal{C}_f \right)$
    \item On a $f\left( 2 \right) = \dfrac{4}{3} \neq 4$ donc le point $D\left( 2 ; 4 \right) \notin \left( \mathcal{C}_f \right)$
\end{itemize}
\end{Solution}

%% SECTION
\section{Parité d'une fonction numérique \duree{15 min}}

%% ACTIVITE
\begin{Activite}[][10 min]

\begin{minipage}[t]{0.65\linewidth}
\begin{enumerate}
    \item Soit $f$ la fonction définie sur $\R$ par $f(x) = 3x^2$ et sa courbe $\mathcal{C}_f$ représentée ci-contre :
        \begin{enumerate}
            \item Vérifier que pour tout $x \in D_f$, $f(-x) = f(x)$
            \item Quelle est la propriété géométrique vérifiée par $\mathcal{C}_f$ ?
        \end{enumerate}
        \vspace{1cm}
    \item Soit $g$ la fonction définie sur $\R^*$ par $g(x) = x^3$ et sa courbe $\mathcal{C}_g$ représentée ci-contre :
        \begin{enumerate}
            \item Vérifier que pour tout $x \in D_g$, $g(-x) = -g(x)$
            \item Quelle est la propriété géométrique vérifiée par $\mathcal{C}_g$ ?
        \end{enumerate}
\end{enumerate}
\end{minipage}
\hfill
\begin{minipage}[t]{0.24\linewidth}
        \vspace{-0.5cm}
        \begin{tikzpicture}[scale=1.2]
            \begin{axis}[
                width=\linewidth,
                height=\linewidth,
                axis lines=middle,
                axis line style={thick, black, -stealth},
                xmin=-2, xmax=2,
                ymin=-0.5, ymax=3.5,
                xtick={-1,1}, ytick={1,2,3},
                tick style={color=black},
                tick label style={font=\footnotesize\sffamily, color=black},
                clip=false
            ]
            \addplot[thick, color=SecondaryColor, smooth, domain=-1.05:1.05, samples=50] {3*x^2};
            \node[right, font=\footnotesize\sffamily\itshape, color=SecondaryColor] at (axis cs: 0.5, 3) {$\mathcal{C}_f$};
            \node[below left, font=\footnotesize\sffamily, color=black] at (axis cs: 0, 0) {$O$};
            \end{axis}
        \end{tikzpicture}
\vspace{0.5cm}
        \begin{tikzpicture}[scale=1.2]
            \begin{axis}[
                width=\linewidth,
                height=\linewidth,
                axis lines=middle,
                axis line style={thick, black, -stealth},
                xmin=-2, xmax=2,
                ymin=-2.5, ymax=2.5,
                xtick={-1,1}, ytick={-1,1},
                tick style={color=black},
                tick label style={font=\footnotesize\sffamily, color=black},
                clip=false
            ]
            \addplot[thick, color=SecondaryColor, smooth, domain=-1.3:1.3, samples=60] {x^3};
            \node[right, font=\footnotesize\sffamily\itshape, color=SecondaryColor] at (axis cs: 0.8, 1) {$\mathcal{C}_g$};
            \node[below right, font=\footnotesize\sffamily, color=black] at (axis cs: 0, 0) {$O$};
            \end{axis}
        \end{tikzpicture}
\end{minipage}%

\end{Activite}

%% SOLUTION_ACTIVITE
\begin{Solution}
\begin{enumerate}
    \item \textbf{a) Vérifions que pour tout $x \in D_f$, $f\left( -x \right) = f\left( x \right)$ :}
    \begin{align*}
        f\left( -x \right) &= 3 \times \left( -x \right)^2
        = 3 \times x^2 =f(x)
    \end{align*}
    \item \textbf{b) Propriété géométrique vérifiée par $\mathscr{C}_f$ :}

    La propriété géométrique est que l'axe des ordonnées est un \textbf{axe de symétrie} pour la courbe $\mathscr{C}_f$.
\end{enumerate}

\begin{enumerate}
    \item \textbf{a) Vérifions que pour tout $x \in D_g$, $g\left( -x \right) = -g\left( x \right)$ :}
    \begin{align*}
        g\left( -x \right) &= \left( -x \right)^3
        = -x^3 = -g(x)
    \end{align*}
    \item \textbf{b) Propriété géométrique vérifiée par $\mathscr{C}_g$ :}

        La propriété géométrique est que l'origine du repère (le point $O$) est un \textbf{centre de symétrie} pour la courbe $\mathscr{C}_g$.
\end{enumerate}
\end{Solution}

%% DEFINITION
\begin{Definition}[][]
Soit $f$ une fonction et $D_f$ son ensemble de définition.
\begin{itemize}
    \item $f$ est \textbf{paire} si et seulement si : pour tout $x \in D_f$ : $-x \in D_f$ et $f(-x) = f(x)$
    \item $f$ est \textbf{impaire} si et seulement si : pour tout $x \in D_f$ : $-x \in D_f$ et $f(-x) = -f(x)$
\end{itemize}
\end{Definition}

%% REMARQUE
\begin{Remarque}[][]
 $x \in D_f$ et $-x \in D_f$ signifie que $D_f$ est symétrique par rapport à zéro.
\end{Remarque}

%% EXEMPLE
\begin{Exemple}[][10 min]
\begin{enumerate}
    \item Soit $f$ la fonction définie par :  $f(x) = x^4 - x^2$
    \vspace{0.1cm} \\
    On a $D_f = \mathbb{R}$. Il est clair que si $x \in \mathbb{R}$ alors $-x \in \mathbb{R}$ \\
    et on a $$f(-x) = (-x)^4 - (-x)^2 = x^4 - x^2$$
    Donc $f$ est paire.

    \item Soit $g$ la fonction définie par : $g(x) = x + x^3$
    \vspace{0.1cm} \\
    On a $D_g = \mathbb{R}$ est symétrique par rapport à $0$ \\
    et on a $$g(-x) = -x + (-x)^3 = -x - x^3 = -(x + x^3) = -g(x)$$
    Donc $g$ est une fonction impaire.
\end{enumerate}
\end{Exemple}

%% APPLICATION
\begin{Application}[][10 min]
Étudier la parité de la fonction $f$ dans chacun des cas suivants :
    \begin{enumerate}
    \item $f(x) = x^2 - 3|x| + 2$
    \item $f(x) = 2x^3 - 5x$
    \item $f(x) = \sqrt{x - 1}$
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Étude de la fonction $f\left( x \right) = x^2 - 3|x| + 2$}

    On a :  $D_f = \mathbb{R}$

    L'ensemble $\mathbb{R}$ est symétrique par rapport à $0$.

    Soit $x \in \mathbb{R}$.
    \begin{align*}
         f\left( -x \right) = \left( -x \right)^2 - 3|-x| + 2
                            = x^2 - 3|x| + 2
    \end{align*}

         La fonction $f$ est \textbf{paire}.

    \vspace{0.5cm}

    \item \textbf{Étude de la fonction $f\left( x \right) = 2x^3 - 5x$}

    \textbf{Étape 1 : Ensemble de définition}

    On a : ~ $D_f = \mathbb{R}$ \quad (fonction polynôme)


    Soit $x \in \mathbb{R}$.
    \begin{align*}
        f\left( -x \right) = 2\left( -x \right)^3 - 5\left( -x \right)
         = -2x^3 + 5x
         &= -\left( 2x^3 - 5x \right) \\
        &= -f\left( x \right)
    \end{align*}

        Donc la fonction $f$ est \textbf{impaire}.

    \vspace{0.5cm}

    \item \textbf{Étude de la fonction $f\left( x \right) = \sqrt{x - 1}$}

     On a : ~ $D_f = \left\{ x \in \mathbb{R} \mid x - 1 \geq 0 \right\}=
     \left\{ x \in \mathbb{R} \mid x  \geq 1 \right\}$

    L'ensemble de définition est donc : $D_f = \left[ 1 ; +\infty \right[$

    $D_f$ n'est pas symétrique par rapport à $0$. \\
    Par conséquent, la fonction $f$ n'est \textbf{ni paire ni impaire}.

\end{enumerate}
\end{Solution}

%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction définie par : $f(x) = x^2 + x$
\begin{enumerate}
    \item Calculer $f(1)$ et $f(-1)$.
    \item En déduire que $f$ n'est ni paire, ni impaire.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}

    % Question 1
    \item \textbf{Calculons $f\left(1\right)$ et $f\left(-1\right)$}
    \begin{itemize}
        \item $f\left(1\right) = \left(1\right)^2 + \left(1\right)
     = 1 + 1 \
     = 2$
        \item $f\left(-1\right) = \left(-1\right)^2 + \left(-1\right)
     = 1 - 1
     = 0$
    \end{itemize}

    % Question 2
    \item \textbf{Déduisons que $f$ n'est ni paire, ni impaire}

    \begin{itemize}
        \item on a trouvé $f\left(-1\right) = 0$ et $f\left(1\right) = 2$.
        Puisque $0 \neq 2$,

        nous remarquons que :
        $f\left(-1\right) \neq f\left(1\right)$

        La fonction n'est donc pas paire.

        \item  nous remarquons que :
        $f\left(-1\right) \neq -f\left(1\right)$

        La fonction n'est donc pas impaire.
    \end{itemize}

    Conclusion : La fonction $f$ n'est ni paire, ni impaire.

\end{enumerate}
\end{Solution}

%% PROPRIETE
\begin{Propriete}[][5 min]
Soit $f$ une fonction et $(C_f)$ sa courbe dans un repère orthogonal $(O ; \vec{i} ; \vec{j})$.
\begin{itemize}
    \item $f$ est paire signifie que $(C_f)$ est symétrique par rapport à l'\textbf{axe des ordonnées}.
    \item $f$ est impaire signifie que $(C_f)$ est symétrique par rapport au point $O$ l'\textbf{origine du repère}.
\end{itemize}
\end{Propriete}

\begin{center}
\begin{tikzpicture}

\begin{scope}[xshift=0cm]
  \draw[->, thick, black] (-3.3,0) -- (3.3,0) node[right, font=\small\sffamily] {$x$};
  \draw[->, thick, black] (0,-0.3) -- (0,3.0) node[above, font=\small\sffamily] {$y$};
  \node[below left, font=\small\sffamily, color=black] at (0,0) {$O$};

  \draw[thick, color=SecondaryColor, smooth, samples=100,
        domain=-2.6:2.6]
    plot (\x, {0.4*\x*\x + 0.3});

  \def\xz{1.8}
  \pgfmathsetmacro{\yz}{0.4*\xz*\xz + 0.3}

  \filldraw[black] (\xz, 1.596) circle (2.2pt);
  \filldraw[black] (-\xz, 1.596) circle (2.2pt);

  \draw[dotted, color=black!40, line width=0.8pt]
    (-\xz, 1.596) -- (\xz, 1.596);

  \filldraw[AccentColor] (0, 1.596) circle (1.8pt);

  \draw[thick, color=AccentColor] (-0.9, 1.506) -- (-0.9, 1.686);
  \draw[thick, color=AccentColor] (-0.75, 1.506) -- (-0.75, 1.686);
  \draw[thick, color=AccentColor] (0.75, 1.506) -- (0.75, 1.686);
  \draw[thick, color=AccentColor] (0.9, 1.506) -- (0.9, 1.686);

  \node[right, font=\small\sffamily, color=black] at (\xz, 1.596) {$M(x\,;\,f(x))$};
  \node[left,  font=\small\sffamily, color=black] at (-\xz, 1.596) {$M'(-x\,;\,f(x))$};

  \node[font=\small\bfseries\sffamily, color=SecondaryColor]
    at (0, -0.9) {Symétrie axiale};
  \node[font=\small\sffamily, color=black]
    at (0, -1.35) {(axe des ordonnées)};
  \node[font=\small\sffamily\itshape, color=SecondaryColor]
    at (2.2, 2.75) {$\mathcal{C}_f$};
\end{scope}

\begin{scope}[xshift=8cm]
  \draw[->, thick, black] (-3.3,0) -- (3.3,0) node[right, font=\small\sffamily] {$x$};
  \draw[->, thick, black] (0,-1) -- (0,2.5) node[above, font=\small\sffamily] {$y$};
  \node[below left, font=\small\sffamily, color=black] at (0,0) {$O$};

  \draw[thick, color=SecondaryColor, smooth, samples=120,
        domain=-2:2]
    plot (\x, {0.5*\x*\x*\x - 0.8*\x});

  \filldraw[AccentColor] (0,0) circle (1.5pt);

  \def\xzb{1.6}
  \filldraw[black] (\xzb, 0.768) circle (2.2pt);
  \filldraw[black] (-\xzb, -0.768) circle (2.2pt);

  \draw[dotted, color=black!40, line width=0.8pt]
    (-\xzb, -0.768) -- (\xzb, 0.768);

  \draw[thick, color=AccentColor]
    ({0.72 - 0.433*0.13}, {0.346 + 0.901*0.13}) -- ({0.72 + 0.433*0.13}, {0.346 - 0.901*0.13});
  \draw[thick, color=AccentColor]
    ({0.88 - 0.433*0.13}, {0.422 + 0.901*0.13}) -- ({0.88 + 0.433*0.13}, {0.422 - 0.901*0.13});
  \draw[thick, color=AccentColor]
    ({-0.72 - 0.433*0.13}, {-0.346 + 0.901*0.13}) -- ({-0.72 + 0.433*0.13}, {-0.346 - 0.901*0.13});
  \draw[thick, color=AccentColor]
    ({-0.88 - 0.433*0.13}, {-0.422 + 0.901*0.13}) -- ({-0.88 + 0.433*0.13}, {-0.422 - 0.901*0.13});

  \node[above right, font=\small\sffamily, color=black] at (\xzb, 0.768)  {$M(x\,;\,g(x))$};
  \node[below left,  font=\small\sffamily, color=black] at (-\xzb, -0.768) {$M'(-x\,;\,-g(x))$};

  \node[font=\small\bfseries\sffamily, color=SecondaryColor]
    at (0, -1.4) {Symétrie centrale};
  \node[font=\small\sffamily, color=black]
    at (0, -1.8) {(par rapport à l'origine $O$)};
  \node[font=\small\sffamily\itshape, color=SecondaryColor]
    at (2.3, 2) {$\mathcal{C}_g$};
\end{scope}

\end{tikzpicture}
\end{center}

%% APPLICATION
\begin{Application}[][10 min]
\noindent
\begin{minipage}[t]{0.45\linewidth}
\vspace{0pt}
Soit $f$ une fonction paire définie sur $[-6 ; 6]$ par

sa courbe $\mathcal{C}_f$ représentée ci-contre :
\begin{enumerate}
    \item Compléter la construction de $(C_f)$.
    \item Déterminer $f(6)$.
    \item Calculer $f(-3)$ et $f(3)$.
\end{enumerate}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.55\linewidth}
\vspace{0pt}
\begin{center}
\begin{tikzpicture}[x=0.72cm, y=0.72cm]
    \draw[black!12, step=1, solid] (-6.5,-2.5) grid (6.5,3.5);
    \draw[->, >=stealth, thick, black] (-6.5,0) -- (6.5,0)
        node[below right, font=\small\sffamily] {$x$};
    \draw[->, >=stealth, thick, black] (0,-2.5) -- (0,3.5)
        node[left, font=\small\sffamily] {$y$};
    \foreach \x in {-6,-5,-4,-3,-2,-1,1,2,3} {
        \draw[PrimaryColor] (\x, 0.08) -- (\x, -0.08);
    }
    \node[below, font=\small\sffamily, color=black] at (-6, -0.15) {$-6$};
    \node[below, font=\small\sffamily, color=black] at (-5, -0.15) {$-5$};
    \node[above left, font=\small\sffamily, color=black] at (-2,  0.1) {$-2$};
    \node[above right, font=\small\sffamily, color=black] at ( 2,  0.1) {$2$};
    \node[below left, font=\small\sffamily, color=black] at (0, 0) {$O$};
    \foreach \y in {-1,1,2,3} {
        \draw[PrimaryColor] (0.08, \y) -- (-0.08, \y);
    }
    \node[right, font=\small\sffamily, color=black] at (0.12, -1) {$-1$};
    \node[right, font=\small\sffamily, color=black] at (0.12,  1) {$1$};
    \node[right, font=\small\sffamily, color=black] at (0.12,  2) {$2$};
    \node[right, font=\small\sffamily, color=black] at (0.12,  3) {$3$};
    \draw[very thick, SecondaryColor] (-6, 3) -- (-2, -1);
    \draw[very thick, SecondaryColor] (0, 2) -- (2, -1);
    \filldraw[black] (-6,  3) circle (2pt);
    \filldraw[black] (-2, -1) circle (2pt);
    \filldraw[black] ( 0, -1) circle (2pt);
    \filldraw[black] ( 0,  2) circle (2pt);
    \filldraw[black] ( 2, -1) circle (2pt);
    \node[right, font=\small\sffamily\itshape, color=SecondaryColor]
        at (4, 2) {$(C_f)$};
\end{tikzpicture}
\end{center}
\end{minipage}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\noindent
\begin{enumerate}

    % Question 1
    \item \textbf{Complétons la construction de la courbe $\left(C_f\right)$}

Pour compléter le tracé sur tout l'intervalle $\left[-6 ; 6\right]$, nous allons utiliser la symétrie par rapport à l'axe des ordonnées.


    Voici la courbe complétée (les tracés ajoutés sont en bleu pour bien les repérer) :

    \begin{center}
    \begin{tikzpicture}[x=0.72cm, y=0.72cm]
        % Grille de fond
        \draw[black!12, step=1, solid] (-6.5,-2.5) grid (6.5,3.5);

        % Axes principaux
        \draw[->, >=stealth, thick, black] (-6.5,0) -- (6.5,0) node[below right, font=\small\sffamily] {$x$};
        \draw[->, >=stealth, thick, black] (0,-2.5) -- (0,3.5) node[left, font=\small\sffamily] {$y$};

        % Graduations axe X
        \foreach \x in {-6,-5,-4,-3,-2,-1,1,2,3,4,5,6} {
            \draw[black] (\x, 0.08) -- (\x, -0.08);
        }
        \node[below, font=\small\sffamily] at (-6, -0.15) {$-6$};
        \node[below, font=\small\sffamily] at (-5, -0.15) {$-5$};
        \node[above left, font=\small\sffamily] at (-2,  0.1) {$-2$};
        \node[above right, font=\small\sffamily] at ( 2,  0.1) {$2$};
        \node[below, font=\small\sffamily] at ( 6, -0.15) {$6$};
        \node[below left, font=\small\sffamily] at (0, 0) {$O$};

        % Graduations axe Y
        \foreach \y in {-1,1,2,3} {
            \draw[black] (0.08, \y) -- (-0.08, \y);
        }
        \node[right, font=\small\sffamily] at (0.12, -1) {$-1$};
        \node[right, font=\small\sffamily] at (0.12,  1) {$1$};
        \node[right, font=\small\sffamily] at (0.12,  2) {$2$};
        \node[right, font=\small\sffamily] at (0.12,  3) {$3$};

        % Tracés donnés par l'énoncé (en noir épais)
        \draw[very thick, black] (-6, 3) -- (-2, -1);
        \draw[very thick, black] (0, 2) -- (2, -1);

        % Tracés complétés par symétrie (en bleu)
        \draw[very thick, blue] (0, 2) -- (-2, -1);
        \draw[very thick, blue] (6, 3) -- (2, -1);

        % Points remarquables
        \filldraw[black] (-6,  3) circle (2pt);
        \filldraw[black] (-2, -1) circle (2pt);
        \filldraw[black] ( 0,  2) circle (2pt);
        \filldraw[black] ( 2, -1) circle (2pt);
        \filldraw[blue]  ( 6,  3) circle (2pt);

        % Label courbe
        \node[right, font=\small\sffamily\itshape, color=blue] at (4.2, 2.2) {$\left(C_f\right)$};
    \end{tikzpicture}
    \end{center}

    \vspace{0.3cm}

    % Question 2
    \item \textbf{Déterminons $f\left(6\right)$}

     Puisque  $f$ est paire, donc : $f\left(6\right) = f\left(-6\right)$
    et $f\left(-6\right) = 3$

    D'où : $f\left(6\right)=0$

    % Question 3
    \item \textbf{Calculons $f\left(-3\right)$ et $f\left(3\right)$}

    Utilisons la parité de $f$ pour trouver $f\left(3\right)$.

    On a :  $f\left(3\right) = f\left(-3\right)$.

    Donc : $f\left(-3\right) = 0 \quad \text{et} \quad f\left(3\right) = 0$

\end{enumerate}
\end{Solution}

%% APPLICATION
\begin{Application}[][10 min]
\noindent
\begin{minipage}[t]{0.55\linewidth}
\vspace{0pt}
Soit $f$ une fonction impaire définie sur $D = [-4 ; -1] \cup [1 ; 4]$ :
\begin{enumerate}
    \item Compléter le tracé de $(C_f)$.
    \item Déterminer $f(4)$ et $f(2)$.
\end{enumerate}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.45\linewidth}
\vspace{0pt}
\begin{center}
\begin{tikzpicture}[x=0.72cm, y=0.72cm]
    \draw[black!12, step=1, solid] (-4.8,-0.5) grid (4.7,4.5);
    \draw[->, >=stealth, thick, black] (-5,0) -- (5,0)
        node[below right, font=\small\sffamily] {$x$};
    \draw[->, >=stealth, thick, black] (0,-1) -- (0,4.5)
        node[right, font=\small\sffamily] {$y$};
    \foreach \x in {-4,-3,-2,-1,1,2,3,4} {
        \draw[PrimaryColor] (\x, 0.08) -- (\x, -0.08);
    }
    \node[above left,  font=\small\sffamily, color=black] at (-4, 0) {$-4$};
    \node[below left,  font=\small\sffamily, color=black] at (-2, 0) {$-2$};
    \node[below left,  font=\small\sffamily, color=black] at (-1, 0) {$-1$};
    \node[below right, font=\small\sffamily, color=black] at ( 1, 0) {$1$};
    \node[below right, font=\small\sffamily, color=black] at ( 4, 0) {$4$};
    \node[below left,  font=\small\sffamily, color=black] at ( 0, 0) {$O$};
    \foreach \y in {1,2,4} {
        \draw[PrimaryColor] (0.08, \y) -- (-0.08, \y);
    }
    \node[left, font=\small\sffamily, color=black] at (0,  4) {$4$};
    \node[left, font=\small\sffamily, color=black] at (0,  1) {$1$};
    \draw[very thick, SecondaryColor] (-4, 0) -- (-2, 3) -- (-1, 1);
    \filldraw[black] (-4, 0) circle (2pt);
    \filldraw[black] (-2,  3) circle (2pt);
    \filldraw[black] (-1,  1) circle (2pt);
    \node[right, font=\small\sffamily\itshape, color=SecondaryColor]
        at (-4, 2) {$(C_f)$};
\end{tikzpicture}
\end{center}
\end{minipage}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\noindent

\begin{enumerate}

    % Question 1
    \item \textbf{Complétons le tracé de $\left(C_f\right)$}

     La fonction $f$ est impaire, donc sa courbe est symétrique par rapport à l'origine $O$. Pour tracer la partie manquante sur l'intervalle $\left[1 ; 4\right]$, nous allons utiliser la symétrie centrale.
    \begin{itemize}
        \item Le point $\left(-4 ; 0\right)$ a pour symétrique $\left(4 ; 0\right)$.
        \item Le point $\left(-2 ; 3\right)$ a pour symétrique $\left(2 ; -3\right)$.
        \item Le point $\left(-1 ; 1\right)$ a pour symétrique $\left(1 ; -1\right)$.
    \end{itemize}
    \begin{center}
    \begin{tikzpicture}[x=0.72cm, y=0.72cm]
        % Grille étendue pour inclure la partie négative
        \draw[black!12, step=1, solid] (-4.8,-3.8) grid (4.8,4.5);

        % Axes principaux
        \draw[->, >=stealth, thick, black] (-5,0) -- (5,0) node[below right, font=\small\sffamily] {$x$};
        \draw[->, >=stealth, thick, black] (0,-4) -- (0,4.5) node[right, font=\small\sffamily] {$y$};

        % Graduations X
        \foreach \x in {-4,-3,-2,-1,1,2,3,4} {
            \draw[black] (\x, 0.08) -- (\x, -0.08);
        }
        \node[above left,  font=\small\sffamily] at (-4, 0) {$-4$};
        \node[above right, font=\small\sffamily] at (-2, 0) {$-2$};
        \node[above right, font=\small\sffamily] at (-1, 0) {$-1$};
        \node[above right, font=\small\sffamily] at ( 1, 0) {$1$};
        \node[above right, font=\small\sffamily] at ( 2, 0) {$2$};
        \node[below right, font=\small\sffamily] at ( 4, 0) {$4$};
        \node[below left,  font=\small\sffamily] at ( 0, 0) {$O$};

        % Graduations Y
        \foreach \y in {-3,-2,-1,1,2,3,4} {
            \draw[black] (0.08, \y) -- (-0.08, \y);
        }
        \node[left, font=\small\sffamily] at (0,  4) {$4$};
        \node[left, font=\small\sffamily] at (0,  3) {$3$};
        \node[left, font=\small\sffamily] at (0,  1) {$1$};
        \node[left, font=\small\sffamily] at (0, -1) {$-1$};
        \node[left, font=\small\sffamily] at (0, -3) {$-3$};

        % Courbe donnée (en noir épais)
        \draw[very thick, black] (-4, 0) -- (-2, 3) -- (-1, 1);

        % Points donnés
        \filldraw[black] (-4, 0) circle (2pt);
        \filldraw[black] (-2,  3) circle (2pt);
        \filldraw[black] (-1,  1) circle (2pt);

        % Partie complétée par symétrie (en bleu)
        \draw[very thick, blue] (1, -1) -- (2, -3) -- (4, 0);

        % Points complétés
        \filldraw[blue] (1, -1) circle (2pt);
        \filldraw[blue] (2, -3) circle (2pt);
        \filldraw[blue] (4,  0) circle (2pt);

        % Labels
        \node[right, font=\small\sffamily\itshape, color=black] at (-3.8, 1.5) {$\left(C_f\right)$};
        \node[right, font=\small\sffamily\itshape, color=blue] at (2.2, -1.5) {$\left(C_f\right)$};
    \end{tikzpicture}
    \end{center}

    \vspace{0.3cm}

    % Question 2
    \item \textbf{Déterminons $f\left(4\right)$ et $f\left(2\right)$}

    On a :
    $f\left(4\right) = -f\left(-4\right) \qquad \text{(car la fonction $f$ est impaire)}$

    et $f\left(-4\right) = 0$ \qquad
    Donc : $f\left(4\right) = -0 = 0$

    On a :
    $f\left(2\right) = -f\left(-2\right) \qquad \text{(car la fonction $f$ est impaire)}$

   et $f\left(-2\right) = 3$
    Donc :   $f\left(2\right) = -3$

\end{enumerate}
\end{Solution}

%% SECTION
\section{Variations d'une fonction \duree{15 min}}

%% SECTION
\subsection{Sens de variation d'une fonction \duree{15 min}}

%% DEFINITION
\begin{Definition}[hi][]
Soit $f$ une fonction numérique et $I$ un intervalle inclus dans $D_f$.
\begin{itemize}
    \item $f$ est \textbf{strictement croissante} (resp. croissante) sur $I$ si pour tous réels $a$ et $b$ de $I$ tels que : $a < b$ on a $f(a) < f(b)$ (resp. $f(a) \leq f(b)$).
    \item $f$ est \textbf{strictement décroissante} (resp. décroissante) sur $I$ si pour tous réels $a$ et $b$ de $I$ tels que $a < b$ on a : $f(a) > f(b)$ (resp. $f(a) \geq f(b)$).
    \item $f$ est \textbf{constante} sur $I$ si pour tous réels $a$ et $b$ de $I$ tels que $a < b$ on a $f(a) = f(b)$.
    \item $f$ est \textbf{strictement monotone} (resp. monotone) sur $I$ si elle est strictement croissante (resp. croissante) ou strictement décroissante (resp. décroissante) sur $I$.
\end{itemize}
\end{Definition}

\begin{center}
\begin{tikzpicture}

\begin{scope}[xshift=0cm]
  \draw[->, thick, black] (-0.3, 0) -- (4.5, 0)
      node[right, font=\small\sffamily] {$x$};
  \draw[->, thick, black] (0, -0.3) -- (0, 3.8)
      node[above, font=\small\sffamily] {$y$};
  \node[below left, font=\small\sffamily, color=black] at (0,0) {$O$};

  \draw[thick, color=SecondaryColor, smooth, samples=100, domain=0.3:4.0]
      plot (\x, {3/(1+exp(-2.5*(\x-2)))});

  \pgfmathsetmacro{\ya}{3/(1+exp(-2.5*(1.2-2)))}
  \pgfmathsetmacro{\yb}{3/(1+exp(-2.5*(3.2-2)))}

  \draw[dashed, color=black!40, line width=0.7pt]
      (1.2, 0) -- (1.2, \ya) -- (0, \ya);
  \draw[dashed, color=black!40, line width=0.7pt]
      (3.2, 0) -- (3.2, \yb) -- (0, \yb);

  \filldraw[black] (1.2, \ya) circle (2pt);
  \filldraw[black] (3.2, \yb) circle (2pt);

  \node[below, font=\small\sffamily, color=black] at (1.2, 0) {$a$};
  \node[below, font=\small\sffamily, color=black] at (3.2, 0) {$b$};
  \node[left,  font=\small\sffamily, color=black] at (0, \ya) {$f(a)$};
  \node[left,  font=\small\sffamily, color=black] at (0, \yb) {$f(b)$};
  \node[right, font=\small\sffamily\itshape, color=SecondaryColor] at (3.5, 2.3) {$(C_f)$};

  \node[align=center, font=\footnotesize\sffamily, color=AccentColor,
        draw=AccentColor, rounded corners=2pt, inner sep=3pt]
      at (2.5, 3.5) {Fonction strictement croissante};
  \node[font=\footnotesize\itshape\sffamily, color=black!40] at (2.2, -0.8) {($f$ conserve l'ordre)};
\end{scope}

\begin{scope}[xshift=7.5cm]
  \draw[->, thick, black] (-0.3, 0) -- (4.5, 0)
      node[right, font=\small\sffamily] {$x$};
  \draw[->, thick, black] (0, -0.3) -- (0, 3.8)
      node[above, font=\small\sffamily] {$y$};
  \node[below left, font=\small\sffamily, color=black] at (0,0) {$O$};

  \draw[thick, color=SecondaryColor, smooth, samples=100, domain=0.3:4.0]
      plot (\x, {3/(1+exp(2.5*(\x-2)))});

  \pgfmathsetmacro{\yga}{3/(1+exp(2.5*(1.2-2)))}
  \pgfmathsetmacro{\ygb}{3/(1+exp(2.5*(2.8-2)))}

  \draw[dashed, color=black!40, line width=0.7pt]
      (1.2, 0) -- (1.2, \yga) -- (0, \yga);
  \draw[dashed, color=black!40, line width=0.7pt]
      (2.8, 0) -- (2.8, \ygb) -- (0, \ygb);

  \filldraw[black] (1.2, \yga) circle (2pt);
  \filldraw[black] (2.8, \ygb) circle (2pt);

  \node[below, font=\small\sffamily, color=black] at (1.2, 0) {$a$};
  \node[below, font=\small\sffamily, color=black] at (2.8, 0) {$b$};
  \node[left,  font=\small\sffamily, color=black] at (0, \yga) {$f(a)$};
  \node[left,  font=\small\sffamily, color=black] at (0, \ygb) {$f(b)$};
  \node[right, font=\small\sffamily\itshape, color=SecondaryColor] at (2, 2.5) {$(C_f)$};

  \node[align=center, font=\footnotesize\sffamily, color=AccentColor,
        draw=AccentColor, rounded corners=2pt, inner sep=3pt]
      at (2.6, 3.5) {Fonction strictement décroissante};
  \node[font=\footnotesize\itshape\sffamily, color=black!40] at (2.2, -0.8) {($f$ change l'ordre)};
\end{scope}

\end{tikzpicture}
\end{center}

%% EXEMPLE
\begin{Exemple}[][10 min]
\begin{enumerate}
    \item Soit $f(x) = 2x + 3$ et $D_f = \mathbb{R}$.
    \vspace{0.1cm} \\
    Soient $a$ et $b$ deux réels tels que : $a < b$ \\
    On a $a < b$ donc $2a < 2b$ alors : $2a + 3 < 2b + 3$ \\
    c'est à dire : $f(a) < f(b)$
    \vspace{0.1cm} \\
    D'où $f$ est strictement croissante sur $\mathbb{R}$.

    \item Soit $g(x) = -3x + 2$ et $D_g = \mathbb{R}$.
    \vspace{0.1cm} \\
    Soient $a$ et $b$ de $\mathbb{R}$ tels que : $a < b$ \\
    On a $a < b$  ~~ donc ~~ $-3a > -3b$ \\
    alors : \qquad ~~~~~~~~~ $-3a + 2 > -3b + 2$ \\
    c'est à dire : \quad ~~~~~~~~ $g(a) > g(b)$ \vspace{0.1cm} \\
    D'où $g$ est strictement décroissante sur $\mathbb{R}$.
\end{enumerate}
\end{Exemple}

%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction numérique définie sur $\mathbb{R}$ par : $f(x) = 2x^2 + 3$
\begin{enumerate}
    \item
    \begin{enumerate}
        \item Soient $a$ et $b$ de $[0 ; +\infty[$ tels que $a < b$. Montrer que $f(a) < f(b)$
        \item En déduire la monotonie de $f$ sur $\mathbb{R}^+$.
    \end{enumerate}
    \item Étudier la monotonie de $f$ sur $\mathbb{R}^-$.
    \item Donner le tableau de variations de $f$ sur $\R$.
\end{enumerate}
\end{Application}

%% SOLUTION_EXERCICE
\begin{Solution}
\begin{enumerate}
    \item 
    \begin{enumerate}
        \item \textbf{Montrons que $f(a) < f(b)$ :}
        
        Soient $a$ et $b$ de $[0 ; +\infty[$ tels que $a < b$.
        On a :
        \begin{align*}
            a &< b \\
            \text{ équivaut à } \quad a^2 &< b^2 \quad \text{(car $a \in \mathbb{R}^+$ et $b \in \mathbb{R}^+$)} \\
            \text{ équivaut à } \quad 2a^2 &< 2b^2 \\
            \text{ équivaut à } \quad 2a^2 + 3 &< 2b^2 + 3
        \end{align*}
        D'où :
        \[ f(a) < f(b) \]

        \item \textbf{Déduisons la monotonie de $f$ sur $\mathbb{R}^+$ :}
        
        Puisque pour tous $a$ et $b$ de $\mathbb{R}^+$ tels que $a < b$, on a $f(a) < f(b)$, la fonction $f$ conserve le sens de l'inégalité.
        Donc : \quad la fonction $f$ est strictement croissante sur $\mathbb{R}^+$.
    \end{enumerate}

    \item \textbf{Étudions la monotonie de $f$ sur $\mathbb{R}^-$ :}
    
    Soient $a$ et $b$ de $]-\infty ; 0]$ tels que $a < b$.
    On a :
    \begin{align*}
        a &< b \\
        \text{ équivaut à } \quad a^2 &> b^2 \quad \text{(car $a \in \mathbb{R}^-$ et $b \in \mathbb{R}^-$)} \\
        \text{ équivaut à } \quad 2a^2 &> 2b^2 \\
        \text{ équivaut à } \quad 2a^2 + 3 &> 2b^2 + 3
    \end{align*}
    D'où :
    \[ f(a) > f(b) \]
    Puisque pour tous $a$ et $b$ de $\mathbb{R}^-$ tels que $a < b$, on a $f(a) > f(b)$, la fonction $f$ change le sens de l'inégalité.
    Donc : \quad la fonction $f$ est strictement décroissante sur $\mathbb{R}^-$.

    \item \textbf{Dressons le tableau de variations de $f$ sur $\mathbb{R}$ :}

    On a $f(0) = 2 \times 0^2 + 3 = 3$.

\begin{center}
\begin{tikzpicture}
\tkzTabInit[lgt=1, espcl=4]{ $x$ / 0.9 , $f(x)$ / 2}{ $-\infty$ , $0$ , $+\infty$ }
\tkzTabVar{ +/ , -/ $3$, +/ }
\end{tikzpicture}
\end{center}
    
\end{enumerate}
\end{Solution}


%% SECTION
\subsection{Taux de variations d'une fonction \duree{15 min}}

%% DEFINITION
\begin{Definition}[hi][]
Soit $f$ une fonction, et soit $a$ et $b$ deux éléments distincts de $D_f$. \\
Le nombre réel $T(a;b) = \frac{f(a)-f(b)}{a-b}$ est appelé \textbf{taux de variations} de $f$ entre $a$ et $b$.
\end{Definition}

%% EXEMPLE
\begin{Exemple}[][10 min]
Soit $f$ la fonction définie par $f(x) = -5x + 3 \quad$ et $\quad D_f = \mathbb{R}$.
\vspace{0.1cm} \\
Soit $a$ et $b$ deux éléments de $\mathbb{R}$ tels que $a \neq b$. On a :
\[ T(a;b) = \frac{f(a)-f(b)}{a-b} = \frac{(-5a+3) - (-5b+3)}{a-b} = \frac{-5(a-b)}{a-b} = -5 \]
\end{Exemple}

%% PROPRIETE
\begin{Propriete}[][]
Soit $f$ une fonction et $T$ son taux de variation entre deux réels distincts $a$ et $b$ d'un intervalle $I$ inclus dans $D_f$.
\begin{itemize}
    \item Si $T > 0$ (resp. $T \geq 0$) pour tous $a$ et $b$ de $I$, alors $f$ est strictement croissante (resp. croissante) sur $I$.
    \item Si $T < 0$ (resp. $T \leq 0$) pour tous $a$ et $b$ de $I$, alors $f$ est strictement décroissante (resp. décroissante) sur $I$.
\end{itemize}
\end{Propriete}

%% EXEMPLE
\begin{Exemple}[][10 min]
Soit $f$ la fonction numérique définie par : $f(x) = x^2 + x$
\vspace{0.1cm} \\
Soit $a$ et $b$ deux réels distincts de $\R$. On a  :
\[ T(a ; b) = \frac{f(a) - f(b)}{a - b}  = \frac{(a^2 + a) - (b^2 + b)}{a - b} = \frac{a^2 - b^2 + a - b}{a - b}
 = \frac{(a - b)(a + b) + (a - b)}{a - b}
 = \frac{(a - b)(a + b + 1)}{a - b} \]
Puisque $a \neq b$, on a $a - b \neq 0$, on peut donc simplifier :
 $T(a ; b) = a + b + 1$

\medskip

\textbf{La monotonie sur l'intervalle } $\left]-\infty ; -\frac{1}{2}\right]$ \textbf{:}

Soit $a$ et $b$ deux réels distincts de $\left]-\infty ; -\frac{1}{2}\right]$,

On a :~~ $a \leq -\frac{1}{2}$ ~et~ $b < -\frac{1}{2}$  (Car $a \neq b$)

Donc : \qquad  $a + b < -1$

C'est-à-dire :  $a + b + 1 < 0$

Donc : $T(a ; b) < 0$, et par suite :

$f$ est strictement décroissante sur $\left]-\infty ; -\frac{1}{2}\right]$.

\medskip

\textbf{La monotonie sur l'intervalle } $\left[-\frac{1}{2} ; +\infty\right[$ \textbf{:}

Soit $a$ et $b$ deux réels distincts de $\left[-\frac{1}{2} ; +\infty\right[$.

On a $a \geq -\frac{1}{2}$ et $b > -\frac{1}{2}$ \quad (Car $a \neq b$)

Donc :~~~ $a + b > -1$

C'est-à-dire : ~ $a + b + 1 > 0$

Donc : $T(a ; b) > 0$, et par suite :

$f$ est strictement croissante sur $\left[-\frac{1}{2} ; +\infty\right[$.

Ces résultats peut être résumés dans un tableau, appelé \textcolor{purple}{Tableau de variations} de la fonction $f$.
\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
\tkzTabInit[lgt=1, espcl=4]{ $x$ / 0.9 , $f(x)$ / 2}{ $-\infty$ , $-\frac{1}{2}$ , $+\infty$ }
\tkzTabVar{ +/ , -/ $-\frac{1}{4}$, +/  }
\end{tikzpicture}
\end{center}

\end{Exemple}

%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction définie sur $\mathbb{R}$ par : $f(x) = x^2 - 6x + 5$
\begin{enumerate}
    \item Soit $a$ et $b$ deux réels distincts. Montrer que $T(a;b) = a+b-6$
    \item Étudier la monotonie de $f$ sur chacun des deux intervalles $]-\infty ; 3]$ et $[3 ; +\infty[$.
    \item Dresser le tableau de variations de $f$.
    \item Comparer les nombres $f(2026)$ et $f(2027)$.
\end{enumerate}
\end{Application}

%% SOLUTION_EXERCICE
\begin{Solution}
\begin{enumerate}
    \item \textbf{Montrons que $T(a;b) = a+b-6$ :}
    
    Soit $a$ et $b$ deux réels distincts.
    On a :
    \begin{align*}
        T(a;b) &= \frac{f(a) - f(b)}{a - b} \\
        &= \frac{(a^2 - 6a + 5) - (b^2 - 6b + 5)}{a - b} \\
        &= \frac{a^2 - 6a + 5 - b^2 + 6b - 5}{a - b} \\
        &= \frac{a^2 - b^2 - 6a + 6b}{a - b} \\
        &= \frac{(a-b)(a+b) - 6(a-b)}{a - b} \\
        &= \frac{(a-b)(a+b-6)}{a - b} \\
        &= a + b - 6 \quad \text{(car } a \neq b \text{)}
    \end{align*}
    D'où :
    \[ T(a;b) = a+b-6 \]

    \item \textbf{Étudions la monotonie de $f$ sur chacun des deux intervalles $]-\infty ; 3]$ et $[3 ; +\infty[$ :}
    
    Soit $a$ et $b$ deux réels distincts de l'intervalle $]-\infty ; 3]$.
    On a :
    $\begin{cases}
        a \leq 3 \\
        b < 3
    \end{cases} \quad \text{alors} \quad a + b < 6$

    Donc : \quad $T(a;b) < 0$
    
    D'où : la fonction $f$ est strictement décroissante sur $]-\infty ; 3]$.

    Soit $a$ et $b$ deux réels distincts de l'intervalle $[3 ; +\infty[$.
    On a :
    $\begin{cases}
        a \geq 3 \\
        b > 3
    \end{cases} \quad \text{alors} \quad a + b > 6$

    Donc : \quad $T(a;b) > 0$
    
    D'où : la fonction $f$ est strictement croissante sur $[3 ; +\infty[$.

    \item \textbf{Dressons le tableau de variations de $f$ :}
    On a :
    \begin{align*}
        f(3) &= (3)^2 - 6(3) + 5 \\
        &= 9 - 18 + 5 \\
        &= -9 + 5 \\
        &= -4
    \end{align*}
    Donc : \quad $f(3) = -4$

\begin{center}
  \begin{tikzpicture}
% Initialisation du tableau avec les largeurs de colonnes (lgt) et l'espacement (espcl)
\tkzTabInit[lgt=2, espcl=3]{$x$ / 1 , $f(x)$ / 2}{$-\infty$, $3$, $+\infty$}

% Ligne des variations
% +/ signifie en haut (sans texte pour les limites)
% -/ -4 signifie en bas avec la valeur -4
\tkzTabVar{+/ , -/ $-4$ , +/ }
\end{tikzpicture}
\end{center}

    \item \textbf{Comparons les nombres $f(2026)$ et $f(2027)$ :}
    
    On a : $2026 \in [3 ; +\infty[$ et $2027 \in [3 ; +\infty[$.
    \begin{align*}
        2026 &< 2027
    \end{align*}
    Puisque la fonction $f$ est strictement croissante sur l'intervalle $[3 ; +\infty[$, elle conserve le sens de l'inégalité.
    Donc : \quad $f(2026) < f(2027)$
    
    D'où :
    \[ f(2026) < f(2027) \]

\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}[][10 min]
\noindent
\begin{minipage}[t]{0.7\linewidth}
\vspace{0pt}
La représentation graphique ci-contre, représente la courbe d'une fonction $f$.

Répondre, par lecture graphique aux questions suivantes :
\begin{enumerate}
    \item Préciser l'ensemble de définition $D_f$ de $f$.
    \item Déterminer les images par $f$ de $-1$ ; $0$ ; $1$ et $2$.
    \item Dresser le tableau de variation de $f$ sur $D_f$.
\end{enumerate}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.35\linewidth}
\vspace{0pt}
\begin{center}
\begin{tikzpicture}[x=0.85cm, y=0.85cm]
    \draw[black!12, step=1, solid] (-1.5,-2.5) grid (3.6,2.8);
    \draw[->, >=stealth, thick, black] (-2,0) -- (4,0)
        node[below right, font=\small\sffamily] {$x$};
    \draw[->, >=stealth, thick, black] (0,-2.5) -- (0,3)
        node[left, font=\small\sffamily] {$y$};
    \foreach \x in {-1,1,2,3} {
        \draw[PrimaryColor] (\x, 0.07) -- (\x, -0.07);
    }
    \node[below left,  font=\small\sffamily] at (-1, 0) {$-1$};
    \node[below right, font=\small\sffamily] at ( 1, 0) {$1$};
    \node[below right, font=\small\sffamily] at ( 2, 0) {$2$};
    \node[below right, font=\small\sffamily] at ( 3, 0) {$3$};
    \node[below left,  font=\small\sffamily] at (  0, 0) {$O$};
    \foreach \y in {-2,-1,1,2} {
        \draw[PrimaryColor] (0.07, \y) -- (-0.07, \y);
    }
    \node[left, font=\small\sffamily] at (0,  2) {$2$};
    \node[left, font=\small\sffamily] at (0,  1) {$1$};
    \node[left, font=\small\sffamily] at (0, -1) {$-1$};
    \node[left, font=\small\sffamily] at (0, -2) {$-2$};
    \draw[very thick, SecondaryColor, smooth, samples=100, domain=-1:3]
        plot (\x, {\x*\x*\x - 3*\x*\x + 2});
    \filldraw[black] (-1, -2) circle (2pt);
    \filldraw[black] ( 3,  2) circle (2pt);
    \node[above right, font=\small\sffamily\itshape, color=SecondaryColor]
        at (0.3, 1.7) {$(C_f)$};
\end{tikzpicture}
\end{center}
\end{minipage}
\end{Application}


%% SOLUTION_EXERCICE
\begin{Solution}
\begin{enumerate}
    \item \textbf{Précisons l'ensemble de définition $D_f$ de $f$ :}
    
    Par lecture graphique, nous constatons que la courbe de la fonction $f$ est définie pour les abscisses allant de $-1$ à $3$ (les deux extrémités étant incluses).
    
    Donc : \quad $D_f = [-1 ; 3]$

    \item \textbf{Déterminons les images par $f$ de $-1$ ; $0$ ; $1$ et $2$ :}
    
    Par lecture graphique des coordonnées des points de la courbe, on a :
    \begin{align*}
        f(-1) &= -2 \\
        f(0) &= 2 \\
        f(1) &= 0 \\
        f(2) &= -2
    \end{align*}

    \item \textbf{Dressons le tableau de variation de $f$ sur $D_f$ :}
    
    Par lecture graphique, nous constatons que la fonction $f$ est strictement croissante sur l'intervalle $[-1 ; 0]$, strictement décroissante sur l'intervalle $[0 ; 2]$, puis strictement croissante sur l'intervalle $[2 ; 3]$.

    \textbf{Conclusion :}
    \[
    \begin{tikzpicture}
    \tkzTabInit[lgt=2,espcl=2.5]{$x$ / 1 , $f(x)$ / 2}{$-1$ , $0$ , $2$ , $3$}
    \tkzTabVar{-/ $-2$ , +/ $2$ , -/ $-2$ , +/ $2$}
    \end{tikzpicture}
    \]
\end{enumerate}
\end{Solution}

%% SECTION
\subsection{Monotonie et parité d'une fonction \duree{15 min}}

%% PROPRIETE
\begin{Propriete}[][5 min]
Soit $f$ une fonction dont $D_f$ est symétrique par rapport à $0$. \\
Soit $I$ un intervalle de $\mathbb{R}$ tel que $I \subset D_f$ et $I'$ le symétrique de $I$ par rapport à $0$.
\begin{enumerate}
    \item[(1)] Dans le cas où $f$ est \textbf{paire}, on a :
    \begin{itemize}
        \item Si $f$ est croissante sur $I$, alors $f$ est décroissante sur $I'$.
        \item Si $f$ est décroissante sur $I$, alors $f$ est croissante sur $I'$.
    \end{itemize}
    \item[(2)] Dans le cas où $f$ est \textbf{impaire}, alors $f$ a le même sens de variation sur $I$ et $I'$.
\end{enumerate}
\end{Propriete}

%% EXEMPLE
\begin{Exemple}[][10 min]
\begin{itemize}
    \item La fonction $f : x \mapsto |x|$ est paire ; elle est croissante sur $I = [0 ; +\infty[$ et décroissante sur $I' = ]-\infty ; 0]$.
    \item La fonction $g : x \mapsto \frac{1}{x}$ est impaire, elle est décroissante sur chacun des intervalles $]-\infty ; 0[$ et $]0 ; +\infty[$.
\end{itemize}
\end{Exemple}

%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction numérique définie par son tableau de variations :

\vspace{0.3cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=1.4]{$x$ / 0.7, $f(x)$ / 1.9}{$-7$, $-5$, $0$, $5$, $7$}
    \tkzTabVar{+/ , -/ $4$, +D/ / }
\end{tikzpicture}
\end{center}
\vspace{0.2cm}

\begin{enumerate}
    \item Déterminer $D_f$.
    \item Recopier et compléter le tableau ci-dessus sachant que $f$ est paire.
    \item Recopier et compléter le tableau ci-dessus sachant que $f$ est impaire.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
\item \textbf{Déterminons $D_f$ :}

 $D_f = [-7 ; 0[ \cup ]0 ; 7]$

\item \textbf{Complétons le tableau sachant que $f$ est paire :}

Puisque $f$ est paire, elle change donc de sens de variation sur les intervalles symétriques par rapport à $0$, et on a : $f(5) = f(-5) = 4$.

\[
\begin{tikzpicture}
\tkzTabInit[lgt=1.5, espcl=1.8]{$x$ / 1, $f(x)$ / 2}{$-7$, $-5$, $0$, $5$, $7$}
\tkzTabVar{+/ , -/ $4$, +D+/ / , -/ $4$, +/ }
\end{tikzpicture}
\]

\item \textbf{Complétons le tableau sachant que $f$ est impaire :}

Puisque $f$ est impaire, elle conserve donc les mêmes variations sur les intervalles symétriques par rapport à $0$, et on a : $f(5) = -f(-5) = -4$.

\[
\begin{tikzpicture}
\tkzTabInit[lgt=1.5, espcl=1.8]{$x$ / 1, $f(x)$ / 2}{$-7$, $-5$, $0$, $5$, $7$}
\tkzTabVar{+/ , -/ $4$, +D-/ / , +/ $-4$, -/ }
\end{tikzpicture}
\]
\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}[][15 min]
Soit $f$ une fonction numérique définie par :
$$f(x) = x + \frac{4}{x}$$

\begin{enumerate}
    \item Déterminer $D_f$ ; puis montrer que $f$ est impaire.
    \item Soit $a$ et $b$ de $\R^*$ tel que $a \neq b$.
    Montrer que :
    $$\frac{f(b) - f(a)}{b - a} = \frac{ab - 4}{ab}$$
    \item Étudier les variations de $f$ sur chacun des intervalles $]0 ; 2]$ et $[2 ; +\infty[$.
    \item En déduire les variations de $f$ sur chacun des intervalles $]-\infty ; -2]$ et $[-2 ; 0[$.
    \item Dresser le tableau de variations de $f$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
\item \textbf{Déterminons $D_f$ ; puis montrons que $f$ est impaire :}

On a : \quad $D_f = \mathbb{R}^*$

\begin{itemize}
    \item $D_f$ est symétrique par rapport à $0$
    \item Et on a pour tout $x$ de $D_f$ :
\begin{align*}
    f(-x) &= (-x) + \frac{4}{-x} \\
          &= -x - \frac{4}{x} \\
          &= -\left(x + \frac{4}{x}\right) \\
          &= -f(x)
\end{align*}
\end{itemize}

Donc la fonction $f$ est \textbf{impaire}.

\item \textbf{Montrons que $\dfrac{f(b) - f(a)}{b - a} = \dfrac{ab - 4}{ab}$ :}

Soit $a$ et $b$ de $\mathbb{R}^*$ tels que $a \neq b$.
On a :  $T(a\,;\,b) = \frac{f(b) - f(a)}{b - a}$ 

Calculons $f(b) - f(a)$ :
\begin{align*}
    f(b) - f(a) &= \left(b + \frac{4}{b}\right) - \left(a + \frac{4}{a}\right) \\
                &= b - a + \frac{4}{b} - \frac{4}{a} \\
                &= (b - a) + \frac{4a - 4b}{ab} \\
                &= (b - a) - \frac{4(b - a)}{ab} \\
                &= (b - a)\left(1 - \frac{4}{ab}\right) \\
                &= (b - a) \cdot \frac{ab - 4}{ab}
\end{align*}
Donc : 
\[ T(a\,;\,b) = \frac{f(b) - f(a)}{b - a}
             = \frac{(b - a) \cdot \dfrac{ab - 4}{ab}}{b - a}
             = \frac{ab - 4}{ab} \quad \text{(car } a \neq b \text{)} \]

D'où : \quad $\dfrac{f(b) - f(a)}{b - a} = \dfrac{ab - 4}{ab}$

\item \textbf{Étudions les variations de $f$ sur chacun des intervalles $]0 ; 2]$ et $[2 ; +\infty[$ :}

\begin{itemize}
    \item Soit $a$ et $b$ de $]0 ; 2]$ tels que $a \neq b$.
On a :
$\begin{cases}
    0 < a \leq 2 \\
    0 < b < 2
\end{cases} \quad \text{alors} \quad 0 < ab < 4$

On a donc : \quad $ab - 4 < 0$

Puisque $ab > 0$ et $ab - 4 < 0$, nous constatons que $\dfrac{ab - 4}{ab} < 0$.

Donc : \quad le taux de variation $T(a;b) < 0$.

D'où : la fonction $f$ est strictement décroissante sur $]0 ; 2]$.

    \item Soit $a$ et $b$ de $[2 ; +\infty[$ tels que $a \neq b$.
On a :
$\begin{cases}
    a \geq 2 \\
    b > 2
\end{cases} \quad \text{alors} \quad ab > 4$

On a donc : \quad $ab - 4 > 0$

Puisque $ab > 0$ et $ab - 4 > 0$, nous constatons que $\dfrac{ab - 4}{ab} > 0$.

Donc : \quad le taux de variation $T(a;b) > 0$.

D'où : la fonction $f$ est strictement croissante sur $[2 ; +\infty[$.
\end{itemize}



\item \textbf{Déduisons les variations de $f$ sur chacun des intervalles $]-\infty ; -2]$ et $[-2 ; 0[$ :}

Puisque $f$ est impaire, elle conserve les mêmes variations sur les intervalles symétriques par rapport à $0$.

\begin{itemize}
    \item L'intervalle symétrique de $[2 ; +\infty[$ est $]-\infty ; -2]$.

Donc : \quad la fonction $f$ est strictement croissante sur $]-\infty ; -2]$.

\item L'intervalle symétrique de $]0 ; 2]$ est $[-2 ; 0[$.

Donc : \quad la fonction $f$ est strictement décroissante sur $[-2 ; 0[$.
\end{itemize}



\item \textbf{Dressons le tableau de variations de $f$ :}

On a :   $f(2) = 2 + \frac{4}{2} 
         = 2 + 2 
         = 4$

Puisque la fonction $f$ est impaire, on a $f(-2) = -f(2) = -4$

\[
\begin{tikzpicture}
\tkzTabInit[lgt=2, espcl=2]{$x$ / 1 , $f(x)$ / 2}{$-\infty$ , $-2$ , $0$ , $2$ , $+\infty$}
\tkzTabVar{-/ , +/ $-4$ , -D+/ / , -/ $4$ , +/ }
\end{tikzpicture}
\]
\end{enumerate}
\end{Solution}

%% SECTION
\section{Extremums d'une fonction numérique \duree{10 min}}

%% ACTIVITE
\begin{Activite}[][5 min]
Un joueur de foot tire un coup franc. La hauteur du ballon (exprimée en mètres) par rapport au sol en fonction du temps $t$ (exprimé en secondes) est donnée par la fonction $h$ définie sur l'intervalle $[0; 6]$ par :
$$h(t) = -t^2 + 6t$$

\begin{enumerate}
    \item Calculer $h(0)$ et $h(6)$. Que signifient ces deux résultats pour le ballon ?
    \item Calculer la hauteur du ballon après $3$ secondes.
    \item
    \begin{enumerate}
    \item Montrer que pour tout $t \in [0; 6]$, on a : $h(t) - 9 = -(t - 3)^2$
    \item En déduire que pour tout $t \in [0; 6]$, on a : $h(t) \leq 9$
    \end{enumerate}
    \item Quelle est la hauteur maximale atteinte par le ballon ? À quel instant est-elle atteinte ?
\end{enumerate}
\end{Activite}


%% SOLUTION_ACTIVITE
\begin{Solution}
\begin{enumerate}
    \item \textbf{Calculons $h(0)$ et $h(6)$ :}
    
    On a : \quad  $h(0) = -(0)^2 + 6(0)    = 0 + 0   = 0$

    On a :  \quad $h(6) = -(6)^2 + 6(6)   = -36 + 36     = 0$
    
    Donc : \quad $h(6) = 0$ et $h(0) = 0$
    
    \[ \text{Ces deux résultats signifient qu'à l'instant initial } (t=0) \text{ et après 6 secondes } (t=6) \text{, le ballon est au niveau du sol.} \]

    \item \textbf{Calculons la hauteur du ballon après $3$ secondes :}
    
    On a : \quad     $h(3) = -(3)^2 + 6(3)     = -9 + 18      = 9$
    
    Donc : \quad la hauteur du ballon après $3$ secondes est de $9$ mètres.
    
    \item 
    \begin{enumerate}
        \item \textbf{Montrons que pour tout $t \in [0; 6]$, on a : $h(t) - 9 = -(t - 3)^2$ :}
        
        Soit $t \in [0; 6]$.
        On a :
        \begin{align*}
            -(t - 3)^2 &= -\left(t^2 - 2(t)(3) + 3^2\right) \\
            &= -\left(t^2 - 6t + 9\right) \\
            &= -t^2 + 6t - 9 \\
            &= \left(-t^2 + 6t\right) - 9 \\
            &= h(t) - 9
        \end{align*}
        D'où : $h(t) - 9 = -(t - 3)^2$ 

        \item \textbf{Déduisons que pour tout $t \in [0; 6]$, on a : $h(t) \leq 9$ :}
        
        Soit $t \in [0; 6]$.
        On a :
        \begin{align*}
            (t - 3)^2 &\geq 0 \quad \text{(car le carré d'un réel est toujours positif)} \\
            \text{ équivaut à } \quad -(t - 3)^2 &\leq 0 \\
            \text{ équivaut à } \quad h(t) - 9 &\leq 0 \\
            \text{ équivaut à } \quad h(t) &\leq 9
        \end{align*}
        Donc : \quad pour tout $t \in [0; 6]$, on a $h(t) \leq 9$.
    \end{enumerate}

    \item \textbf{Déterminons la hauteur maximale atteinte par le ballon et l'instant où elle est atteinte :}
    
    Puisque pour tout $t \in [0; 6]$, on a $h(t) \leq 9$, et que $h(3) = 9$, 
    
    nous constatons que $9$ est la valeur maximale de la fonction $h$ sur l'intervalle $[0; 6]$.
    
     $\text{La hauteur maximale atteinte par le ballon est de } 9 \text{ mètres, à l'instant } t = 3 \text{ secondes.}$ 
\end{enumerate}
\end{Solution}


%% DEFINITION
\begin{Definition}[][]
Soit $f$ une fonction numérique ; $I$ un intervalle inclus dans $D_f$ et $a \in I$.
\begin{itemize}
    \item On dit que $f(a)$ est le \textbf{maximum} de $f$ sur $I$ si :
    $f(x) \leq f(a)$ pour tout $x$ de $I$.
    \item On dit que $f(a)$ est le \textbf{minimum} de $f$ sur $I$ si :
    $f(x) \geq f(a)$ pour tout $x$ de $I$.
\end{itemize}
\end{Definition}

%% APPLICATION
\begin{Application}[][10 min]
On considère la fonction $f(x) = x^2 - 6x + 7$
\begin{enumerate}
    \item Vérifier que $f(x) \geq -2$ pour tout $x$ de $\mathbb{R}$.
    \item Calculer $f(3)$, puis en déduire que $-2$ est le minimum de $f$ sur $\mathbb{R}$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Vérifions que $f(x) \geq -2$ pour tout $x \in \mathbb{R}$ :}

    On a :
    \begin{align*}
        f(x) - (-2) &= x^2 - 6x + 7 + 2 \\
                    &= x^2 - 6x + 9 \\
                    &= (x - 3)^2
    \end{align*}
    Puisque $(x-3)^2 \geq 0$ pour tout $x \in \mathbb{R}$, on a $f(x) - (-2) \geq 0$.

    D'où : \quad $f(x) \geq -2$ pour tout $x \in \mathbb{R}$.

    \item \textbf{Calculons $f(3)$ :}

    \[ f(3) = 3^2 - 6 \times 3 + 7 = 9 - 18 + 7 = -2 \]

    \textbf{Conclusion :}
    Puisque $f(x) \geq -2$ pour tout $x \in \mathbb{R}$ et $f(3) = -2$, le nombre $-2$ est bien le \textbf{minimum} de $f$ sur $\mathbb{R}$.
\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}[][10 min]
On considère la fonction $f(x) = -2x^2 + 4x + 3$ et $D_f = \mathbb{R}$
\begin{enumerate}
    \item Montrer que pour tout $x$ de $\mathbb{R}$ ; $f(x) \leq 5$.
    \item Calculer $f(1)$, puis en déduire que $5$ est le maximum de $f$ sur $\mathbb{R}$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Montrons que $f(x) \leq 5$ pour tout $x \in \mathbb{R}$ :}

    On a :
    \begin{align*}
        5 - f(x) &= 5 - (-2x^2 + 4x + 3) \\
                 &= 5 + 2x^2 - 4x - 3 \\
                 &= 2x^2 - 4x + 2 \\
                 &= 2(x^2 - 2x + 1) \\
                 &= 2(x - 1)^2
    \end{align*}
    Puisque $2(x-1)^2 \geq 0$ pour tout $x \in \mathbb{R}$, on a $5 - f(x) \geq 0$.

    D'où : \quad $f(x) \leq 5$ pour tout $x \in \mathbb{R}$.

    \item \textbf{Calculons $f(1)$ :}

    \[ f(1) = -2(1)^2 + 4(1) + 3 = -2 + 4 + 3 = 5 \]

    \textbf{Conclusion :}
    Puisque $f(x) \leq 5$ pour tout $x \in \mathbb{R}$ et $f(1) = 5$, le nombre $5$ est bien le \textbf{maximum} de $f$ sur $\mathbb{R}$.
\end{enumerate}
\end{Solution}


%% REMARQUE
\begin{Remarque}[][]
\begin{itemize}
    \item $M$ est le maximum de $f$ sur $I$ si et seulement si :
    \begin{enumerate}
        \item Pour tout $x$ de $I$ : $f(x) \leq M$
        \item L'équation $f(x) = M$ admet au moins une solution dans $I$.
    \end{enumerate}
    \item $m$ est le minimum de $f$ sur $I$ si et seulement si :
    \begin{enumerate}
        \item Pour tout $x$ de $I$ : $f(x) \geq m$
        \item L'équation $f(x) = m$ admet au moins une solution dans $I$.
    \end{enumerate}
\end{itemize}
\end{Remarque}

%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ une fonction définie par : \quad $f(x) = x + \frac{4}{x}$
\begin{enumerate}
    \item Montrer que $4$ est le minimum de $f$ sur $]0 ; +\infty[$.
    \item Montrer que $-4$ est le maximum de $f$ sur $]-\infty ; 0[$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Montrons que $4$ est le minimum de $f$ sur $]0 ; +\infty[$ :}

    Pour tout $x \in \left]0 ; +\infty\right[$, on a :
    \begin{align*}
        f(x) - 4 &= x + \dfrac{4}{x} - 4 \\[4pt]
                 &= \dfrac{x^2 - 4x + 4}{x} \\[4pt]
                 &= \dfrac{(x - 2)^2}{x}
    \end{align*}
    Puisque $(x-2)^2 \geq 0$ et $x > 0$, on a $f(x) - 4 \geq 0$, soit $f(x) \geq 4$.

    De plus : $f(2) = 2 + \dfrac{4}{2} = 4$.

    \textbf{Conclusion :} Le nombre $4$ est le \textbf{minimum} de $f$ sur $\left]0 ; +\infty\right[$.

    \item \textbf{Montrons que $-4$ est le maximum de $f$ sur $]-\infty ; 0[$ :}

    Pour tout $x \in \left]-\infty ; 0\right[$, on a :
    \begin{align*}
        f(x) - (-4) &= x + \dfrac{4}{x} + 4 \\[4pt]
                    &= \dfrac{x^2 + 4x + 4}{x} \\[4pt]
                    &= \dfrac{(x + 2)^2}{x}
    \end{align*}
    Puisque $(x+2)^2 \geq 0$ et $x < 0$, on a $\dfrac{(x+2)^2}{x} \leq 0$, soit $f(x) \leq -4$.

    De plus : $f(-2) = -2 + \dfrac{4}{-2} = -2 - 2 = -4$.

    \textbf{Conclusion :} Le nombre $-4$ est le \textbf{maximum} de $f$ sur $\left]-\infty ; 0\right[$.
\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction numérique définie par : \quad
$f(x) = \frac{x^2 - 3}{2x^2 + 4}$

\begin{enumerate}
  \item Déterminer $D_f$ puis étudier la parité de la fonction $f$.
  \item Calculer les images des nombres suivants $0 \ ; 1 \ $ et $ -1$ par $f$.
  \item
  \begin{enumerate}
    \item Résoudre dans $\mathbb{R}$ l'équation : $f(x) = \frac{1}{2}$
    \item Montrer que, pour tout réel $x$ : $f(x) \leqslant \frac{1}{2}$
    \item Le nombre $\frac{1}{2}$ est-il un maximum de $f$ sur $\mathbb{R}$ ?
  \end{enumerate}
  \item
  \begin{enumerate}
    \item Montrer que pour tout $x$ de $\mathbb{R}$ : $f(x) \geqslant -\frac{3}{4}$
    \item Le nombre $-\frac{3}{4}$ est-il un minimum de $f$ sur $\mathbb{R}$ ?
  \end{enumerate}
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Déterminons $D_f$ :}

    Puisque $2x^2 + 4 = 2(x^2 + 2) > 0$ pour tout $x \in \mathbb{R}$, le dénominateur ne s'annule jamais.

    Donc : \quad $D_f = \mathbb{R}$

    \textbf{Étudions la parité de $f$ :}

    Soit $x \in \mathbb{R}$.
    \begin{align*}
        f(-x) &= \dfrac{(-x)^2 - 3}{2(-x)^2 + 4} = \dfrac{x^2 - 3}{2x^2 + 4} = f(x)
    \end{align*}
    La fonction $f$ est \textbf{paire}.

    \item \textbf{Calculons les images de $0$, $1$ et $-1$ :}

    \begin{align*}
        f(0) &= \dfrac{0 - 3}{0 + 4} = -\dfrac{3}{4} \\[6pt]
        f(1) &= \dfrac{1 - 3}{2 + 4} = \dfrac{-2}{6} = -\dfrac{1}{3} \\[6pt]
        f(-1) &= f(1) = -\dfrac{1}{3} \quad \text{(car la fonction $f$ est paire)}
    \end{align*}

    \item
    \begin{enumerate}
        \item \textbf{Résolvons $f(x) = \dfrac{1}{2}$ :}

        \begin{align*}
            \dfrac{x^2 - 3}{2x^2 + 4} = \dfrac{1}{2}
            &\text{ équivaut à } 2(x^2 - 3) = 2x^2 + 4 \\
            &\text{ équivaut à } 2x^2 - 6 = 2x^2 + 4 \\
            &\text{ équivaut à } -6 = 4
        \end{align*}
        Cette équation est impossible. L'équation $f(x) = \dfrac{1}{2}$ n'a \textbf{aucune solution} dans $\mathbb{R}$.

        \item \textbf{Montrons que $f(x) \leq \dfrac{1}{2}$ pour tout $x \in \mathbb{R}$ :}

        \begin{align*}
            \dfrac{1}{2} - f(x) &= \dfrac{1}{2} - \dfrac{x^2 - 3}{2x^2 + 4} \\[6pt]
                                &= \dfrac{(2x^2 + 4) - 2(x^2 - 3)}{2(2x^2 + 4)} \\[6pt]
                                &= \dfrac{2x^2 + 4 - 2x^2 + 6}{2(2x^2 + 4)} \\[6pt]
                                &= \dfrac{10}{2(2x^2 + 4)}
        \end{align*}
        Puisque $10 > 0$ et $2(2x^2+4) > 0$, on a $\dfrac{1}{2} - f(x) > 0$, donc $f(x) < \dfrac{1}{2}$.

        \item Puisque l'équation $f(x) = \dfrac{1}{2}$ n'admet aucune solution dans $\mathbb{R}$, le nombre $\dfrac{1}{2}$ \textbf{n'est pas un maximum} de $f$ sur $\mathbb{R}$.
    \end{enumerate}

    \item
    \begin{enumerate}
        \item \textbf{Montrons que $f(x) \geq -\dfrac{3}{4}$ pour tout $x \in \mathbb{R}$ :}

        \begin{align*}
            f(x) + \dfrac{3}{4} &= \dfrac{x^2 - 3}{2x^2 + 4} + \dfrac{3}{4} \\[6pt]
                                 &= \dfrac{4(x^2 - 3) + 3(2x^2 + 4)}{4(2x^2 + 4)} \\[6pt]
                                 &= \dfrac{4x^2 - 12 + 6x^2 + 12}{4(2x^2 + 4)} \\[6pt]
                                 &= \dfrac{10x^2}{4(2x^2 + 4)}
        \end{align*}
        Puisque $10x^2 \geq 0$ et $4(2x^2+4) > 0$, on a $f(x) + \dfrac{3}{4} \geq 0$, donc $f(x) \geq -\dfrac{3}{4}$.

        \item Puisque $f(x) \geq -\dfrac{3}{4}$ pour tout $x \in \mathbb{R}$ et $f(0) = -\dfrac{3}{4}$, le nombre $-\dfrac{3}{4}$ est bien le \textbf{minimum} de $f$ sur $\mathbb{R}$, atteint en $x = 0$.
    \end{enumerate}
\end{enumerate}
\end{Solution}



%% SECTION
\section{Fonctions périodiques \duree{15 min}}

%% DEFINITION
\begin{Definition}[][]
Soit $f$ une fonction numérique et $D_f$ son ensemble de définition. \\
On dit que $f$ est \textbf{périodique} s'il existe un réel $T$ strictement positif tel que :
\begin{itemize}
    \item $x + T \in D_f$ ; pour tout $x$ de $D_f$.
    \item $f(x + T) = f(x)$ ; pour tout $x$ de $D_f$.
\end{itemize}

On dit que $T$ est \textbf{une période} de $f$ et que $f$ est \textbf{$T$-périodique}.
\end{Definition}

%% APPLICATION
\begin{Application}[][10 min]
Considérons les fonctions $f$ et $g$ définies sur $\mathbb{R}$ par :
 $f(x) = \cos(x) \quad \text{et} \quad g(x) = \sin(x) $
\begin{enumerate}
    \item Étudier la parité de $f$ et $g$.
    \item Calculer $f(x+2\pi)$ et $g(x+2\pi)$ ; déduire.
    \item Préciser puis compléter les courbes de $f$ et $g$ représentées ci-dessous.
\end{enumerate}

\begin{center}
    \begin{tikzpicture}[x=0.9cm, y=1cm]
        \draw[->, >=stealth, thick] (-7.5,0) -- (7.5,0) node[below right] {$x$};
        \draw[->, >=stealth, thick] (0,-1.5) -- (0,1.5) node[left] {$y$};
        \node[below left] at (0,0) {$O$};
        \draw[dashed, gray!80] (-7.5,1) -- (7,1);
        \draw[dashed, gray!80] (-7.5,-1) -- (7,-1);
        \node[left] at (0,1) {$1$};
        \node[left] at (0,-1) {$-1$};
        \foreach \k/\label in {
            -4/-2\pi, -3/-\frac{3\pi}{2}, -2/-\pi, -1/-\frac{\pi}{2},
             1/\frac{\pi}{2}, 2/\pi, 3/\frac{3\pi}{2}, 4/2\pi
        } {
            \draw (\k*pi/2, 0.1) -- (\k*pi/2, -0.1) node[below, font=\footnotesize] {$\label$};
        }
        \draw[very thick, PrimaryColor, domain=0:pi, samples=100] plot (\x, {cos(\x r)});
    \end{tikzpicture}
    \hfill
    \begin{tikzpicture}[x=0.9cm, y=1cm]
        \draw[->, >=stealth, thick] (-7.5,0) -- (7.5,0) node[below right] {$x$};
        \draw[->, >=stealth, thick] (0,-1.5) -- (0,1.5) node[left] {$y$};
        \node[below left] at (0,0) {$O$};
        \draw[dashed, gray!80] (-7.5,1) -- (7,1);
        \draw[dashed, gray!80] (-7.5,-1) -- (7,-1);
        \node[left] at (0,1) {$1$};
        \node[left] at (0,-1) {$-1$};
        \foreach \k/\label in {
            -4/-2\pi, -3/-\frac{3\pi}{2}, -2/-\pi, -1/-\frac{\pi}{2},
             1/\frac{\pi}{2}, 2/\pi, 3/\frac{3\pi}{2}, 4/2\pi
        } {
            \draw (\k*pi/2, 0.1) -- (\k*pi/2, -0.1) node[below, font=\footnotesize] {$\label$};
        }
        \draw[very thick, PrimaryColor, domain=0:pi, samples=100] plot (\x, {sin(\x r)});
    \end{tikzpicture}
\end{center}

\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Étudions la parité de $f$ et $g$ :}

    Soit $x \in \mathbb{R}$.
    \begin{align*}
        f(-x) &= \cos(-x) = \cos(x) = f(x)
    \end{align*}
    La fonction $f$ est \textbf{paire}.

    \begin{align*}
        g(-x) &= \sin(-x) = -\sin(x) = -g(x)
    \end{align*}
    Donc la fonction $g$ est \textbf{impaire}.

    \item \textbf{Calculons $f(x+2\pi)$ et $g(x+2\pi)$ :}

    \[ f(x + 2\pi) = \cos(x + 2\pi) = \cos(x) = f(x) \]
    \[ g(x + 2\pi) = \sin(x + 2\pi) = \sin(x) = g(x) \]

    \textbf{Conclusion :} Les fonctions $f$ et $g$ sont $2\pi$-\textbf{périodiques}.

    \item \textbf{Complétons les courbes :}

    Puisque $f$ est \textbf{paire}, sa courbe est symétrique par rapport à l'axe des ordonnées : on complète le tracé de $[0\,;\,\pi]$ par symétrie axiale, puis on reproduit par translation de $2k\pi$ ($k \in \mathbb{Z}$).

    Puisque $g$ est \textbf{impaire}, sa courbe est symétrique par rapport à l'origine : on complète le tracé de $[0\,;\,\pi]$ par symétrie centrale en $O$, puis on reproduit par translation de $2k\pi$.
\end{enumerate}
\end{Solution}


%% EXERCICE
\begin{Exercice}[][15 min]
On considère la fonction numérique $f$ définie sur $\mathbb{R}$ par : \quad
$f\big(x\big) = \dfrac{2x}{x^2 + 1}$
\vspace{0.2cm} \\
On note $\big(\mathcal{C}_f\big)$ sa courbe représentative dans un repère orthonormé $\big(O; \vect{i}; \vect{j}\big)$.

\begin{enumerate}
    \item Déterminer $D_f$, puis étudier la parité de $f$.
    \item Calculer $f\big(0\big)$, $f\big(1\big)$ et $f\big(2\big)$.

    \item Soient $a$ et $b$ deux réels distincts de l'intervalle $[0; +\infty[$.
    \begin{enumerate}
        \item Montrer que :
        $T\big(a; b\big) = \dfrac{2\big(1 - ab\big)}{\big(a^2 + 1\big)\big(b^2 + 1\big)}$
        \item Étudier la monotonie de $f$ sur $[0; 1]$ et sur $[1; +\infty[$.
        \item En déduire la monotonie de $f$ sur $[-1; 0]$ et sur $]-\infty; -1]$.
        \item Dresser le tableau de variations de $f$ sur $\R$.
    \end{enumerate}

    \item
    \begin{enumerate}
        \item Montrer que pour tout réel $x \in \mathbb{R}$, on a : $1 - f\big(x\big) = \dfrac{\big(x - 1\big)^2}{x^2 + 1}$
        \item En déduire que pour tout $x \in \mathbb{R}$, on a : $f\big(x\big) \leqslant 1$
    \end{enumerate}
    \item Résoudre dans $\mathbb{R}$ l'équation $f\big(x\big) = 1$
          En déduire que le nombre $1$ est le maximum de $f$ sur $\mathbb{R}$.
    \item En utilisant la parité de $f$, montrer que $-1$ est le minimum de $f$ sur $\mathbb{R}$ et préciser en quel point il est atteint.
\end{enumerate}
\end{Exercice}

%% SOLUTION_EXERCICE
\begin{Solution}
\begin{enumerate}
    \item \textbf{Déterminons $D_f$ et étudions la parité de $f$ :}

    Puisque $x^2 + 1 > 0$ pour tout $x \in \mathbb{R}$, le dénominateur ne s'annule jamais.

    Donc : \quad $D_f = \mathbb{R}$

    Soit $x \in \mathbb{R}$.
    \begin{align*}
        f(-x) &= \dfrac{2(-x)}{(-x)^2 + 1} = \dfrac{-2x}{x^2 + 1} = -f(x)
    \end{align*}
    Donc la fonction $f$ est \textbf{impaire}.

    \item \textbf{Calculons $f(0)$, $f(1)$ et $f(2)$ :}

    \[ f(0) = \dfrac{0}{1} = 0 \qquad f(1) = \dfrac{2}{2} = 1 \qquad f(2) = \dfrac{4}{5} \]

    \item
    \begin{enumerate}
        \item \textbf{Calculons $T(a\,;\,b)$ :}

        \textbf{Étape 1 —} Définition du taux de variation :
        \[ T(a\,;\,b) = \dfrac{f(b) - f(a)}{b - a} \]

        \textbf{Étape 2 —} Calculons $f(b) - f(a)$ séparément :
        \begin{align*}
            f(b) - f(a) &= \dfrac{2b}{b^2 + 1} - \dfrac{2a}{a^2 + 1} \\[6pt]
                        &= \dfrac{2b(a^2 + 1) - 2a(b^2 + 1)}{(a^2 + 1)(b^2 + 1)} \\[6pt]
                        &= \dfrac{2a^2b + 2b - 2ab^2 - 2a}{(a^2 + 1)(b^2 + 1)} \\[6pt]
                        &= \dfrac{2ab(a - b) - 2(a - b)}{(a^2 + 1)(b^2 + 1)} \\[6pt]
                        &= \dfrac{2(a - b)(ab - 1)}{(a^2 + 1)(b^2 + 1)} \\[6pt]
                        &= \dfrac{-2(b - a)(1 - ab)}{(a^2 + 1)(b^2 + 1)}
        \end{align*}

        \textbf{Étape 3 —} Divisons par $(b - a)$ :
        \[ T(a\,;\,b) = \dfrac{f(b) - f(a)}{b - a} = \dfrac{-2(b-a)(1-ab)}{(a^2+1)(b^2+1)(b-a)} = \dfrac{2(1 - ab)}{(a^2 + 1)(b^2 + 1)} \quad \text{(car } a \neq b\text{)} \]

        \item \textbf{Étudions la monotonie de $f$ sur $[0\,;\,1]$ et sur $[1\,;\,+\infty[$ :}

        Soient $a$ et $b$ deux réels distincts de $[0\,;\,+\infty[$ avec $a < b$. On a $(a^2 + 1)(b^2 + 1) > 0$.

        \textbf{Sur $[0\,;\,1]$ :} Pour $0 \leq a < b \leq 1$, on a $ab \leq 1$, donc $1 - ab \geq 0$.

        D'où : $T(a\,;\,b) \geq 0$. Donc $f$ est \textbf{strictement croissante} sur $[0\,;\,1]$.

        \textbf{Sur $[1\,;\,+\infty[$ :} Pour $1 \leq a < b$, on a $ab \geq 1$, donc $1 - ab \leq 0$.

        D'où : $T(a\,;\,b) \leq 0$. Donc $f$ est \textbf{strictement décroissante} sur $[1\,;\,+\infty[$.

        \item \textbf{Déduisons la monotonie sur $[-1\,;\,0]$ et $]-\infty\,;\,-1]$ :}

        Puisque $f$ est \textbf{impaire}, $f$ est strictement croissante sur $[-1\,;\,0]$ et strictement décroissante sur $]-\infty\,;\,-1]$.

        \item \textbf{Tableau de variations de $f$ sur $\mathbb{R}$ :}

        \begin{center}
        \begin{tikzpicture}
            \tkzTabInit[lgt=1.5, espcl=2]{$x$ / 0.7, $f(x)$ / 2.5}{$-\infty$, $-1$, $0$, $1$, $+\infty$}
            \tkzTabVar{+/ , -/ $-1$ , -/ $0$ , +/ $1$ , -/ }
        \end{tikzpicture}
        \end{center}
    \end{enumerate}

    \item
    \begin{enumerate}
        \item \textbf{Montrons que $1 - f(x) = \dfrac{(x-1)^2}{x^2+1}$ :}

        \begin{align*}
            1 - f(x) &= 1 - \dfrac{2x}{x^2 + 1} = \dfrac{x^2 + 1 - 2x}{x^2 + 1} = \dfrac{(x-1)^2}{x^2+1}
        \end{align*}

        \item \textbf{Déduisons que $f(x) \leq 1$ :}

        Puisque $(x-1)^2 \geq 0$ et $x^2 + 1 > 0$, on a $\dfrac{(x-1)^2}{x^2+1} \geq 0$.

        Donc $1 - f(x) \geq 0$, d'où $f(x) \leq 1$.
    \end{enumerate}

    \item \textbf{Résolvons $f(x) = 1$ et concluons :}

    \[ f(x) = 1 \Leftrightarrow \dfrac{(x-1)^2}{x^2+1} = 0 \Leftrightarrow (x-1)^2 = 0 \Leftrightarrow x = 1 \]

    Puisque $f(x) \leq 1$ pour tout $x \in \mathbb{R}$ et $f(1) = 1$, le nombre $1$ est le \textbf{maximum} de $f$ sur $\mathbb{R}$.

    \item \textbf{Le minimum de $f$ sur $\mathbb{R}$ :}

    Puisque $f$ est \textbf{impaire} : pour tout $x \in \mathbb{R}$, $f(-x) = -f(x)$.

    Donc $f(x) = -f(-x) \geq -1$ (car $f(-x) \leq 1$).

    De plus $f(-1) = -f(1) = -1$.

    \textbf{Conclusion :} Le nombre $-1$ est le \textbf{minimum} de $f$ sur $\mathbb{R}$, atteint au point $(-1\,;\,-1)$.
\end{enumerate}
\end{Solution}



%% SECTION
\section{Fonction $x \mapsto ax^2$ ; $(a \in \mathbb{R}^*)$ \duree{15 min}}

%% ACTIVITE
\begin{Activite}[][10 min]
Soit $f$ la fonction définie sur $\mathbb{R}$ par : $f(x) = ax^2$ \quad ($a \in \mathbb{R}^*$)

On note $\big(\mathcal{C}_f\big)$ la courbe de $f$ dans un repère orthonormé $\big(O; \vect{i}; \vect{j}\big)$.
\begin{enumerate}
    \item Étudier la parité de $f$.
    \item Soient $x$ et $y$ deux réels distincts.

    Montrer que : \quad $T(x;y) = a(x+y) $
    \item Étudier la monotonie de $f$ sur $[0 ; +\infty[$.
    \item En déduire la monotonie de $f$ sur $]-\infty ; 0]$.
    \item Dresser le tableau de variations de $f$ sur $\R$.
    \item On pose $a=1$.
    \begin{enumerate}
        \item Compléter le tableau suivant :
        \vspace{0.1cm}
         \begin{center}
            \renewcommand{\arraystretch}{1.2}
            \begin{tabular}{|Sc|*{6}{Sc|}}
                \hline
                $x$    & $-2$ & $-1$ & $0$ & $1$ & $2$  \\ \hline
                $f(x)$ &       &       &     &   &    \\
                \hline
            \end{tabular}
        \end{center}
        \vspace{0.1cm}
        \item Construire $\big(\mathcal{C}_f\big)$.
    \end{enumerate}
\end{enumerate}
\end{Activite}

%% DEFINITION
\begin{Definition}[][]
Soit $a$ un réel non nul.

La représentation graphique de la fonction $x \mapsto ax^2$ est appelée
\textbf{parabole} de sommet $O$ et d'axe de symétrie l'axe des ordonnées.
\end{Definition}

\noindent On distingue deux cas :

\vspace{0.2cm}

\noindent
\begin{minipage}[t]{0.48\linewidth}

\textcolor[HTML]{004DA0}{\textbf{$\bullet$ $1^{er}$ Cas $a > 0$ :}}
\vspace{0.2cm}

$\blacktriangleright$~Le \textbf{tableau de variations} de $f$ est :

\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.3]{$x$ / 0.6, $f(x)$ / 1.8}{$-\infty$, $0$, $+\infty$}
    \tkzTabVar{+/ , -/ $0$ , +/ }
\end{tikzpicture}
\end{center}

\vspace{0.2cm}
Pour tout $x \in \mathbb{R}$ : $f(x) \geq f(0)$

et $f(0) = 0$ donc $f(0)$ est le \textbf{minimum} de $f$ sur $\mathbb{R}$.

\vspace{0.3cm}
$\blacktriangleright$~ La \textbf{courbe} de $f$ :

\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
  \begin{axis}[
      width=6cm, height=5.5cm,
      axis lines=middle,
      axis line style={thick, black, -stealth},
      xmin=-2.3, xmax=2.3,
      ymin=-0.3, ymax=4.3,
      xtick={-1,1}, xticklabels={$-1$,$1$},
      ytick=\empty,
      tick label style={font=\small\sffamily, color=black},
      tick style={color=black},
      clip=false,
  ]
      \addplot[thick, color=SecondaryColor, smooth, domain=-2:2, samples=80]{x^2};
      \node[right, font=\small\sffamily\itshape, color=SecondaryColor]
          at (axis cs:1.8, 3.2) {$(\mathcal{C}_f)$};
      \node[below left, font=\small\sffamily, color=black]
          at (axis cs:0, 0) {$O$};
      \node[right, font=\small\sffamily, color=black]
          at (axis cs:1.1, 1.1) {$a$};
  \end{axis}
\end{tikzpicture}
\end{center}

$(\mathcal{C}_f)$ est la parabole de sommet $O(0;0)$ et dirigée vers le \textbf{haut}.
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\linewidth}
\textcolor[HTML]{004DA0}{\textbf{$\bullet$ $2^{ème}$ Cas $a < 0$ :}}
\vspace{0.2cm}

$\blacktriangleright$~Le \textbf{tableau de variations} de $f$ est :

\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.3]{$x$ / 0.6, $f(x)$ / 1.8}{$-\infty$, $0$, $+\infty$}
    \tkzTabVar{-/ , +/ $0$ , -/ }
\end{tikzpicture}
\end{center}

\vspace{0.2cm}
Pour tout $x \in \mathbb{R}$ : $f(x) \leq f(0)$

et $f(0) = 0$ donc $f(0)$ est le \textbf{maximum} de $f$ sur $\mathbb{R}$.

\vspace{0.3cm}
$\blacktriangleright$~ La \textbf{courbe} de $f$ :

\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
  \begin{axis}[
      width=6cm, height=5.5cm,
      axis lines=middle,
      axis line style={thick, black, -stealth},
      xmin=-2.3, xmax=2.3,
      ymin=-4.3, ymax=0.3,
      xtick={-1,1}, xticklabels={$-1$,$1$},
      ytick=\empty,
      tick label style={font=\small\sffamily, color=black},
      tick style={color=black},
      clip=false,
  ]
      \addplot[thick, color=SecondaryColor, smooth, domain=-2:2, samples=80]{-x^2};
      \node[right, font=\small\sffamily\itshape, color=SecondaryColor]
          at (axis cs:1.8, -3.2) {$(\mathcal{C}_f)$};
      \node[above left, font=\small\sffamily, color=black]
          at (axis cs:0, 0) {$O$};
      \node[right, font=\small\sffamily, color=black]
          at (axis cs:1.1, -1.1) {$a$};
  \end{axis}
\end{tikzpicture}
\end{center}

$(\mathcal{C}_f)$ est la parabole de sommet $O(0;0)$ et dirigée vers le \textbf{bas}.
\end{minipage}

%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction définie sur $\mathbb{R}$ par : $f(x) = 3x^2$
\begin{enumerate}
    \item Calculer $f(1)$ et $f(2)$ ; puis déduire $f(-1)$ et $f(-2)$.
    \item Dresser le tableau de variations de $f$ sur $\mathbb{R}$.
    \item Tracer $(\mathcal{C}_f)$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Calculons $f(1)$ et $f(2)$ ; puis déduisons $f(-1)$ et $f(-2)$ :}

    \[ f(1) = 3 \times 1^2 = 3 \qquad f(2) = 3 \times 4 = 12 \]

    Puisque $f(x) = 3x^2$ est une fonction \textbf{paire} ($f(-x) = 3(-x)^2 = 3x^2 = f(x)$) :
    \[ f(-1) = f(1) = 3 \qquad f(-2) = f(2) = 12 \]

    \item \textbf{Tableau de variations de $f$ sur $\mathbb{R}$ :}

    Puisque $a = 3 > 0$, la parabole est dirigée vers le haut, avec minimum $f(0) = 0$ en $x = 0$.
    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $0$, $+\infty$}
        \tkzTabVar{+/ , -/ $0$ , +/ }
    \end{tikzpicture}
    \end{center}

    \item \textbf{Tracé de $(\mathcal{C}_f)$ :}

    $(\mathcal{C}_f)$ est une \textbf{parabole} de sommet $O(0\,;\,0)$, d'axe de symétrie l'axe des ordonnées, dirigée vers le \textbf{haut}.
\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}[][10 min]
Soit $g$ la fonction définie sur $\mathbb{R}$ par : $g(x) = ax^2$  ~ ($a \in \mathbb{R}^*$)
\begin{enumerate}
    \item Déterminer la valeur de $a$ tel que $g(2) = -3$.
    \item Dresser le tableau de variations de $g$ sur $\mathbb{R}$.
    \item Tracer $(\mathcal{C}_g)$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Déterminons la valeur de $a$ :}

    On a : $g(2) = a \times 2^2 = 4a$.
    \begin{align*}
        g(2) = -3 &\text{ équivaut à } 4a = -3 \text{ équivaut à } a = -\dfrac{3}{4}
    \end{align*}
    Donc : \quad $a = -\dfrac{3}{4}$

    \item \textbf{Tableau de variations de $g$ sur $\mathbb{R}$ :}

    Puisque $a = -\dfrac{3}{4} < 0$, la parabole est dirigée vers le bas, avec maximum $g(0) = 0$ en $x = 0$.
    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1, espcl=2.5]{$x$ / 0.7, $g(x)$ / 2}{$-\infty$, $0$, $+\infty$}
        \tkzTabVar{-/ , +/ $0$ , -/ }
    \end{tikzpicture}
    \end{center}

    \item \textbf{Tracé de $(\mathcal{C}_g)$ :}

    $(\mathcal{C}_g)$ est une \textbf{parabole} de sommet $O(0\,;\,0)$, d'axe de symétrie l'axe des ordonnées, dirigée vers le \textbf{bas}.
\end{enumerate}
\end{Solution}



%% SECTION
\section{Étude de la fonction $x \mapsto ax^2 + bx + c$  ; $(a \in \mathbb{R}^*)$ \duree{15 min}}

%% DEFINITION
\begin{Definition}[][]
Soient $a$, $b$ et $c$ des réels tels que $a \neq 0$. \\
La courbe représentative de la fonction $f : x \mapsto ax^2 + bx + c$ dans un repère orthonormé est
une \textbf{parabole} de sommet $S\!\left(-\dfrac{b}{2a}\,;\,f\!\left(-\dfrac{b}{2a}\right)\right)$
et d'axe de symétrie la droite d'équation $x = -\dfrac{b}{2a}$.
\end{Definition}

%% APPLICATION
\begin{Application}[][10 min]
Déterminer le sommet et l'axe de symétrie de $f$ dans chacun des cas suivants : \\
\textbf{1.} $f(x) = x^2 + 2x + 1$ \qquad
\textbf{2.} $f(x) = -2x^2 + 6x + 1$ \qquad
\textbf{3.} $f(x) = x^2 - 1$
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item $f(x) = x^2 + 2x + 1$ : \quad $a = 1$, $b = 2$, $c = 1$.
    \[ -\dfrac{b}{2a} = -\dfrac{2}{2} = -1 \qquad f(-1) = 1 - 2 + 1 = 0 \]
    Le \textbf{sommet} est $S(-1\,;\,0)$ et l'axe de symétrie est la droite $x = -1$.

    \item $f(x) = -2x^2 + 6x + 1$ : \quad $a = -2$, $b = 6$, $c = 1$.
    \[ -\dfrac{b}{2a} = -\dfrac{6}{-4} = \dfrac{3}{2} \qquad f\!\left(\dfrac{3}{2}\right) = -2 \times \dfrac{9}{4} + 6 \times \dfrac{3}{2} + 1 = -\dfrac{9}{2} + 9 + 1 = \dfrac{11}{2} \]
    Le \textbf{sommet} est $S\!\left(\dfrac{3}{2}\,;\,\dfrac{11}{2}\right)$ et l'axe de symétrie est la droite $x = \dfrac{3}{2}$.

    \item $f(x) = x^2 - 1$ : \quad $a = 1$, $b = 0$, $c = -1$.
    \[ -\dfrac{b}{2a} = 0 \qquad f(0) = -1 \]
    Le \textbf{sommet} est $S(0\,;\,-1)$ et l'axe de symétrie est la droite $x = 0$ (axe des ordonnées).
\end{enumerate}
\end{Solution}


%% PROPRIETE
\begin{Propriete}[][5 min]
Soient $a$, $b$ et $c$ des réels tels que $a \neq 0$. \\
Le tableau de variations de la fonction $f : x \mapsto ax^2 + bx + c$ est :

\vspace{0.4cm}
\noindent
\begin{minipage}[t]{0.48\linewidth}
\begin{center}
\textbf{$\bullet$ Si $a > 0$ :}
\end{center}
\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.6]{$x$ / 1, $f(x)$ / 2.5}%
        {$-\infty$, $-\dfrac{b}{2a}$, $+\infty$}
    \tkzTabVar{+/ , -/ $f\!\left(-\dfrac{b}{2a}\right)$ , +/ }
\end{tikzpicture}
\end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\linewidth}
\begin{center}
\textbf{$\bullet$ Si $a < 0$ :}
\end{center}
\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.6]{$x$ / 1, $f(x)$ / 2.5}%
        {$-\infty$, $-\dfrac{b}{2a}$, $+\infty$}
    \tkzTabVar{-/ , +/ $f\!\left(-\dfrac{b}{2a}\right)$ , -/ }
\end{tikzpicture}
\end{center}
\end{minipage}
\end{Propriete}

%% APPLICATION
\begin{Application}[][10 min]
Dresser le tableau de variations de $f$ dans chacun des cas suivants : \\
\textbf{1.} $f(x) = x^2 + 2x + 1$ \qquad
\textbf{2.} $f(x) = -2x^2 + 6x + 1$ \qquad
\textbf{3.} $f(x) = x^2 - 1$
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item $f(x) = x^2 + 2x + 1$, $a = 1 > 0$, sommet $S(-1\,;\,0)$ :
    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $-1$, $+\infty$}
        \tkzTabVar{+/ , -/ $0$ , +/ }
    \end{tikzpicture}
    \end{center}

    \item $f(x) = -2x^2 + 6x + 1$, $a = -2 < 0$, sommet $S\!\left(\dfrac{3}{2}\,;\,\dfrac{11}{2}\right)$ :
    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $\frac{3}{2}$, $+\infty$}
        \tkzTabVar{-/ , +/ $\frac{11}{2}$ , -/ }
    \end{tikzpicture}
    \end{center}

    \item $f(x) = x^2 - 1$, $a = 1 > 0$, sommet $S(0\,;\,-1)$ :
    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $0$, $+\infty$}
        \tkzTabVar{+/ , -/ $-1$ , +/ }
    \end{tikzpicture}
    \end{center}
\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}[][10 min]
Soit $f$ la fonction définie sur $\mathbb{R}$ par $f(x) = 2x^2 - 4x + 3$
\begin{enumerate}
    \item Déterminer la nature de $(C_f)$ en précisant ses éléments caractéristiques.
    \item Dresser le tableau de variations de $f$
    \item Construire la courbe de $f$
    \item Discuter suivant les valeurs de $m$ les solutions de l'équation $f(x) = m \quad (m \in \mathbb{R})$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Déterminons la nature de $(C_f)$ :}

    On a $a = 2$, $b = -4$, $c = 3$.

    Puisque $a = 2 \neq 0$, la courbe $(C_f)$ est une \textbf{parabole}.
    \[ -\dfrac{b}{2a} = -\dfrac{-4}{4} = 1 \qquad f(1) = 2 - 4 + 3 = 1 \]
    Le \textbf{sommet} est $S(1\,;\,1)$ et l'axe de symétrie est la droite $x = 1$.

    Puisque $a = 2 > 0$, la parabole est dirigée vers le \textbf{haut}.

    Le discriminant est : $\Delta = (-4)^2 - 4 \times 2 \times 3 = 16 - 24 = -8 < 0$.

    Puisque $\Delta < 0$, la parabole ne coupe pas l'axe des abscisses.

    \item \textbf{Tableau de variations de $f$ :}

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $1$, $+\infty$}
        \tkzTabVar{+/ , -/ $1$ , +/ }
    \end{tikzpicture}
    \end{center}

    \item \textbf{Construction de $(C_f)$ :}

    $(C_f)$ est une parabole de sommet $S(1\,;\,1)$, d'axe $x = 1$, dirigée vers le haut.

    \item \textbf{Discussion de l'équation $f(x) = m$ :}

    L'équation $f(x) = m$ représente l'intersection de la parabole $(C_f)$ avec la droite $y = m$.

    Puisque le minimum de $f$ est $1$ (atteint en $x = 1$) :
    \begin{itemize}
        \item Si $m < 1$ : l'équation n'a \textbf{aucune solution}.
        \item Si $m = 1$ : l'équation a \textbf{une seule solution} : $x = 1$.
        \item Si $m > 1$ : l'équation a \textbf{deux solutions distinctes}.
    \end{itemize}
\end{enumerate}
\end{Solution}


%% REMARQUE
\begin{Remarque}[][]
La courbe représentative de la fonction $(a \neq 0) \; f : x \mapsto ax^2 + bx + c$ peut être construite à partir de la courbe représentative de la fonction $x \mapsto ax^2$ en utilisant la translation de vecteur $\vec{u} \left( -\frac{b}{2a} ; f\left(-\frac{b}{2a}\right) \right)$.
\end{Remarque}


%% SECTION
\section{Fonction $x \mapsto \dfrac{a}{x}$ ; $(a \in \mathbb{R}^*)$ \duree{15 min}}

%% ACTIVITE
\begin{Activite}[][10 min]
Soit $f$ la fonction définie sur $\mathbb{R^*}$ par : $f(x) = \frac{a}{x}$ \quad ($a \in \mathbb{R}^*$)

On note $\big(\mathcal{C}_f\big)$ la courbe de $f$ dans un repère orthonormé $\big(O; \vect{i}; \vect{j}\big)$.
\begin{enumerate}
    \item Vérifier que $f$ est impaire.
    \item Soient $x$ et $y$ deux réels distincts de $]0; +\infty[$ .

    Montrer que : \quad $T(x;y) = -\frac{a}{xy} $
    \item Étudier la monotonie de $f$ sur $]0 ; +\infty[$.
    \item En déduire la monotonie de $f$ sur $]-\infty ; 0[$.
    \item Dresser le tableau de variations de $f$ sur $\R^*$.
    \item On pose $a=1$.
    \begin{enumerate}
        \item Compléter le tableau suivant :
        \vspace{0.1cm}
         \begin{center}
            \renewcommand{\arraystretch}{1.2}
            \begin{tabular}{|Sc|*{6}{Sc|}}
                \hline
                $x$    & $-2$ & $-1$ & $0$ & $1$ & $2$  \\ \hline
                $f(x)$ &       &       &     &   &    \\
                \hline
            \end{tabular}
        \end{center}
        \vspace{0.1cm}
        \item Construire $\big(\mathcal{C}_f\big)$.
    \end{enumerate}
\end{enumerate}
\end{Activite}

%% DEFINITION
\begin{Definition}[][]
Soit $a$ un nombre réel non nul.

Soit $(O; \vec{i}, \vec{j})$ un repère orthogonal du plan.
La représentation graphique de la fonction $x \mapsto \frac{a}{x}$ est appelée \textbf{hyperbole} de centre $O$, d'asymptotes les droites d'équations $x = 0$ et $y = 0$ (axe du repère).
\end{Definition}

\noindent On distingue deux cas :

\vspace{0.2cm}

\noindent
\begin{minipage}[t]{0.48\linewidth}

\textcolor[HTML]{004DA0}{\textbf{$\bullet$ $1^{er}$ Cas $a < 0$ :}}
\vspace{0.2cm}

$\blacktriangleright$~Le \textbf{tableau de variations} de $f$ est :

\vspace{0.2cm}
\begin{center}
 \begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.5]{$x$ / 0.7 , $f(x)$ / 2}{$-\infty$, $0$, $+\infty$}
    \tkzTabVar{-/ , +D-/ , +/}
    \end{tikzpicture}
\end{center}

\vspace{0.3cm}
$\blacktriangleright$~ La \textbf{courbe} de $f$ :

\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        proplot,
        width=7.5cm, height=7cm,
        xmin=-4.5, xmax=4.5,
        ymin=-4.5, ymax=4.5,
        xtick=\empty, ytick=\empty,
        xlabel={$x$}, ylabel={$y$},
        clip=false
    ]
        \node[anchor=north east, font=\sffamily, PrimaryColor] at (0,0) {$O$};
        \addplot[domain=-4.5:-0.22, samples=100] {-1/x}
            node[pos=0.8, right=-25pt] {$(C_f)$};
        \addplot[domain=0.22:4.5, samples=100] {-1/x};
    \end{axis}
\end{tikzpicture}
\end{center}

\end{minipage}
\hfill
\begin{minipage}[t]{0.48\linewidth}
\textcolor[HTML]{004DA0}{\textbf{$\bullet$ $2^{ème}$ Cas $a > 0$ :}}
\vspace{0.2cm}

$\blacktriangleright$~Le \textbf{tableau de variations} de $f$ est :

\vspace{0.2cm}
\begin{center}
    \begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.5]{$x$ / 0.7 , $f(x)$ / 2}{$-\infty$, $0$, $+\infty$}
    \tkzTabVar{+/ , -D+/ , -/}
    \end{tikzpicture}
\end{center}

\vspace{0.3cm}
$\blacktriangleright$~ La \textbf{courbe} de $f$ :

\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \begin{axis}[
        proplot,
        width=7.5cm, height=7cm,
        xmin=-4.5, xmax=4.5,
        ymin=-4.5, ymax=4.5,
        xtick=\empty, ytick=\empty,
        xlabel={$x$}, ylabel={$y$},
        clip=false
    ]
        \node[anchor=north east, font=\sffamily, PrimaryColor] at (0,0) {$O$};
        \addplot[domain=-4.5:-0.22, samples=100] {1/x};
        \addplot[domain=0.22:4.5, samples=100] {1/x}
            node[pos=0.2, right=2pt] {$(C_f)$};
    \end{axis}
\end{tikzpicture}
\end{center}

\end{minipage}

%% APPLICATION
\begin{Application}
Soit $f$ la fonction définie par : $f(x) = \frac{3}{x}$

\begin{enumerate}
    \item Donner le tableau de variations de $f$.
    \item Construire la courbe $(\mathcal{C}_f)$ dans un repère orthonormal $(O; \vec{i}, \vec{j})$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Tableau de variations de $f$ :}

    Puisque $a = 3 > 0$, la fonction $f(x) = \dfrac{3}{x}$ est \textbf{strictement décroissante} sur $]-\infty\,;\,0[$ et sur $]0\,;\,+\infty[$.

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $0$, $+\infty$}
        \tkzTabVar{+/ $0^-$ , -D+/ , -/ $0^+$ }
    \end{tikzpicture}
    \end{center}

    \item \textbf{Construction de $(\mathcal{C}_f)$ :}

    $(\mathcal{C}_f)$ est une \textbf{hyperbole} de centre $O(0\,;\,0)$, d'asymptotes les axes du repère.
\end{enumerate}
\end{Solution}


%% APPLICATION
\begin{Application}
Soit $f$ la fonction définie par : $f(x) = -\frac{2}{x}$

\begin{enumerate}
    \item Donner le tableau de variations de $f$.
    \item Construire la courbe $(\mathcal{C}_f)$ dans un repère orthonormal $(O; \vec{i}, \vec{j})$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Tableau de variations de $f$ :}

    Puisque $a = -2 < 0$, la fonction $f(x) = \dfrac{-2}{x}$ est \textbf{strictement croissante} sur $]-\infty\,;\,0[$ et sur $]0\,;\,+\infty[$.

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $0$, $+\infty$}
        \tkzTabVar{-/ $0^+$ , +D-/ , +/ $0^-$ }
    \end{tikzpicture}
    \end{center}

    \item \textbf{Construction de $(\mathcal{C}_f)$ :}

    $(\mathcal{C}_f)$ est une \textbf{hyperbole} de centre $O(0\,;\,0)$, d'asymptotes les axes du repère.
\end{enumerate}
\end{Solution}



%% SECTION
\section{La fonction homographique $x \mapsto \dfrac{ax+b}{cx+d}$ ; ($c \neq 0$ et $ad-bc \neq 0$)\duree{25 min}}

%% DEFINITION
\begin{Definition}
\begin{itemize}
    \item Toute fonction $f$ qui peut s'écrire sous la forme $f(x) = \frac{ax+b}{cx+d}$ où $a, b, c, d$ sont des réels tels que $c \neq 0$ et $ad-bc \neq 0$ est appelée fonction \textbf{homographique}.
    \item On note $\Delta$ le \textbf{déterminant} de $f$ et on a : $\Delta = \begin{vmatrix} a & b \\ c & d \end{vmatrix} = ad - bc$
\end{itemize}
\end{Definition}

%% EXEMPLE
\begin{Exemple}
\begin{enumerate}
    \item Soit $f$ la fonction définie par : $f(x) = \frac{2x+1}{x-3}$

    On a $\Delta = \begin{vmatrix} 2 & 1 \\ 1 & -3 \end{vmatrix} = 2(-3) - 1(1) = -7 \neq 0$

    Donc $f$ est une fonction homographique.

    \item Soit $g$ la fonction définie par : $g(x) = \frac{x}{x-1}$

    On a  $\Delta = \begin{vmatrix} 1 & 0 \\ 1 & -1 \end{vmatrix} = 1(-1) - 1(0) = -1 \neq 0$

    Donc $g$ est une fonction homographique.
\end{enumerate}
\end{Exemple}

%% PROPRIETE
\begin{Propriete}[Variations]

Pour étudier les variations d'une fonction homographique, on calcule son déterminant $\Delta$ :

\vspace{0.2cm}
\noindent
\begin{minipage}[t]{0.48\linewidth}
\begin{center}
\textbf{$\bullet$ Si $\Delta < 0$ :}
\end{center}
\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.6]{$x$ / 1, $f(x)$ / 2.5}{$-\infty$, $-\frac{d}{c}$, $+\infty$}
    \tkzTabVar{+/ , -D+/ , -/}
\end{tikzpicture}
\end{center}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\linewidth}
\begin{center}
\textbf{$\bullet$ Si $\Delta > 0$ :}
\end{center}
\vspace{0.2cm}
\begin{center}
\begin{tikzpicture}
    \tkzTabInit[lgt=1, espcl=2.6]{$x$ / 1, $f(x)$ / 2.5}{$-\infty$, $-\frac{d}{c}$, $+\infty$}
    \tkzTabVar{-/ , +D-/ , +/}
\end{tikzpicture}
\end{center}
\end{minipage}
\end{Propriete}

%% APPLICATION
\begin{Application}
Donner le tableau des variations des fonctions suivantes :
    \begin{itemize}
    \item $f(x) = \frac{3x-1}{x+1}$
    \item $g(x) = \frac{x}{2x+3}$
    \item $h(x) = \frac{-3}{3x-1}$
\end{itemize}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{itemize}
    \item $f(x) = \dfrac{3x-1}{x+1}$ : \quad $a = 3$, $b = -1$, $c = 1$, $d = 1$.

    $\Delta = ad - bc = 3 \times 1 - (-1) \times 1 = 4 > 0$

    Puisque $\Delta > 0$, $f$ est \textbf{strictement croissante} sur $]-\infty\,;\,-1[$ et sur $]-1\,;\,+\infty[$.

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $-1$, $+\infty$}
        \tkzTabVar{-/ , +D-/ , +/ }
    \end{tikzpicture}
    \end{center}

    \item $g(x) = \dfrac{x}{2x+3}$ : \quad $a = 1$, $b = 0$, $c = 2$, $d = 3$.

    $\Delta = ad - bc = 1 \times 3 - 0 \times 2 = 3 > 0$

    Le pôle est $x = -\dfrac{d}{c} = -\dfrac{3}{2}$.

    Puisque $\Delta > 0$, $g$ est \textbf{strictement croissante} sur $\left]-\infty\,;\,-\dfrac{3}{2}\right[$ et sur $\left]-\dfrac{3}{2}\,;\,+\infty\right[$.

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $g(x)$ / 2}{$-\infty$, $-\frac{3}{2}$, $+\infty$}
        \tkzTabVar{-/ , +D-/ , +/ }
    \end{tikzpicture}
    \end{center}

    \item $h(x) = \dfrac{-3}{3x-1}$ : \quad $a = 0$, $b = -3$, $c = 3$, $d = -1$.

    $\Delta = ad - bc = 0 \times (-1) - (-3) \times 3 = 9 > 0$

    Le pôle est $x = -\dfrac{d}{c} = \dfrac{1}{3}$.

    Puisque $\Delta > 0$, $h$ est \textbf{strictement croissante} sur $\left]-\infty\,;\,\dfrac{1}{3}\right[$ et sur $\left]\dfrac{1}{3}\,;\,+\infty\right[$.

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $h(x)$ / 2}{$-\infty$, $\frac{1}{3}$, $+\infty$}
        \tkzTabVar{-/ , +D-/ , +/ }
    \end{tikzpicture}
    \end{center}
\end{itemize}
\end{Solution}


%% PROPRIETE
\begin{Propriete}[Courbe]
La courbe représentative de la fonction homographique est une \textbf{hyperbole} de centre $\Omega\left(-\dfrac{d}{c}, \dfrac{a}{c}\right)$ et d'asymptotes la droite verticale $x=-\dfrac{d}{c}$ et la droite horizontale $y=\dfrac{a}{c}$.
\end{Propriete}

%% APPLICATION
\begin{Application}
Soit $f$ la fonction définie par $f(x) = \frac{2x+1}{x-1}$
\begin{enumerate}
    \item Déterminer $D_f$.
    \item Déterminer la nature de $(C_f)$ en précisant ses éléments caractéristiques.
    \item Construire $(C_f)$.
\end{enumerate}
\end{Application}

%% SOLUTION_APPLICATION
\begin{Solution}
\begin{enumerate}
    \item \textbf{Déterminons $D_f$ :}

    \begin{align*}
        D_f &= \left\{ x \in \mathbb{R} \mid x - 1 \neq 0 \right\} = \left\{ x \in \mathbb{R} \mid x \neq 1 \right\}
    \end{align*}
    Donc : \quad $D_f = \mathbb{R} \setminus \{1\} = \left]-\infty\,;\,1\right[ \cup \left]1\,;\,+\infty\right[$

    \item \textbf{Nature de $(C_f)$ et éléments caractéristiques :}

    On a $a = 2$, $b = 1$, $c = 1$, $d = -1$.

    $\Delta = ad - bc = 2 \times (-1) - 1 \times 1 = -3 < 0$

    La courbe $(C_f)$ est une \textbf{hyperbole} de centre :
    \[ \Omega\!\left(-\dfrac{d}{c}\,;\,\dfrac{a}{c}\right) = \Omega(1\,;\,2) \]

    Les asymptotes sont :
    \begin{itemize}
        \item Asymptote \textbf{verticale} : $x = 1$
        \item Asymptote \textbf{horizontale} : $y = 2$
    \end{itemize}

    Puisque $\Delta = -3 < 0$, $f$ est \textbf{strictement décroissante} sur $]-\infty\,;\,1[$ et sur $]1\,;\,+\infty[$.

    \item \textbf{Construction de $(C_f)$ :}

    $(C_f)$ est une hyperbole de centre $\Omega(1\,;\,2)$, d'asymptotes $x = 1$ et $y = 2$.
\end{enumerate}
\end{Solution}


%% EXERCICE
\begin{Exercice}
On considère les fonctions numériques $f$ et $g$ définies par : \quad
$f(x) = -x^2 + 4x - 3  \quad \text{et} \quad  g(x) = \frac{x-3}{x-2}$
\vspace{0.2cm} \\
On note $(C_f)$ et $(C_g)$ leurs courbes représentatives dans un repère orthonormé $(O;\vec{i};\vec{j})$.

\begin{enumerate}
\item
\begin{enumerate}
    \item Déterminer la nature de $(C_f)$ en précisant ses éléments caractéristiques.
    \item Calculer $f(0)$, $f(1)$ et $f(3)$.
    \item Dresser le tableau de variations de la fonction $f$ sur $\mathbb{R}$.
\end{enumerate}
\item
\begin{enumerate}
    \item Déterminer $D_g$, l'ensemble de définition de la fonction $g$.
    \item Dresser le tableau de variations de la fonction $g$ sur $D_g$.
    \item Déterminer la nature de $(C_g)$ en précisant ses éléments caractéristiques.
\end{enumerate}
\item
\begin{enumerate}
    \item Montrer que pour tout $x \in D_g$, on a :
    $f(x) - g(x) = \frac{(x-3)(-x^2+3x-3)}{x-2}$
    \item Montrer que pour tout réel $x$ : $-x^2+3x-3 < 0 $
    \item En déduire les coordonnées du point d'intersection des courbes $(C_f)$ et $(C_g)$.
\end{enumerate}
\item Tracer les asymptotes, la courbe $(C_g)$ et la courbe $(C_f)$ dans le même repère $(O;\vec{i};\vec{j})$.
\item  Par lecture graphique, résoudre dans $\mathbb{R}$ l'inéquation :
$\frac{x-3}{x-2} \le -x^2 + 4x - 3$
\item  Discuter graphiquement, suivant les valeurs du paramètre réel $m$, le nombre de solutions de l'équation :$$-x^2 + 4x - 3 - m = 0$$
\end{enumerate}
\end{Exercice}

%% SOLUTION_EXERCICE
\begin{Solution}
\begin{enumerate}
    \item
    \begin{enumerate}
        \item \textbf{Nature de $(C_f)$ :}

        On a $a = -1$, $b = 4$, $c = -3$.

        Puisque $a \neq 0$, $(C_f)$ est une \textbf{parabole}.
        \[ -\dfrac{b}{2a} = -\dfrac{4}{-2} = 2 \qquad f(2) = -4 + 8 - 3 = 1 \]
        Le \textbf{sommet} est $S(2\,;\,1)$, l'axe de symétrie est la droite $x = 2$.

        Puisque $a = -1 < 0$, la parabole est dirigée vers le \textbf{bas}.

        \item \textbf{Calculons $f(0)$, $f(1)$ et $f(3)$ :}

        \[ f(0) = -3 \qquad f(1) = -1 + 4 - 3 = 0 \qquad f(3) = -9 + 12 - 3 = 0 \]

        \item \textbf{Tableau de variations de $f$ :}

        \begin{center}
        \begin{tikzpicture}
            \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $f(x)$ / 2}{$-\infty$, $2$, $+\infty$}
            \tkzTabVar{-/ , +/ $1$ , -/ }
        \end{tikzpicture}
        \end{center}
    \end{enumerate}

    \item
    \begin{enumerate}
        \item \textbf{Déterminons $D_g$ :}

        \begin{align*}
            D_g = \left\{ x \in \mathbb{R} \mid x - 2 \neq 0 \right\} = \left\{ x \in \mathbb{R} \mid x \neq 2 \right\}
        \end{align*}
        Donc : \quad $D_g = \mathbb{R} \setminus \{2\} = \left]-\infty\,;\,2\right[ \cup \left]2\,;\,+\infty\right[$

        \item \textbf{Tableau de variations de $g$ :}

        On a $a = 1$, $b = -3$, $c = 1$, $d = -2$.

        $\Delta = ad - bc = 1 \times (-2) - (-3) \times 1 = -2 + 3 = 1 > 0$

        Puisque $\Delta > 0$, $g$ est \textbf{strictement croissante} sur $]-\infty\,;\,2[$ et sur $]2\,;\,+\infty[$.

        \begin{center}
        \begin{tikzpicture}
            \tkzTabInit[lgt=1.2, espcl=2.5]{$x$ / 0.7, $g(x)$ / 2}{$-\infty$, $2$, $+\infty$}
            \tkzTabVar{-/ , +D-/ , +/ }
        \end{tikzpicture}
        \end{center}

        \item \textbf{Nature de $(C_g)$ :}

        $(C_g)$ est une \textbf{hyperbole} de centre $\Omega(2\,;\,1)$, d'asymptotes : $x = 2$ (verticale) et $y = 1$ (horizontale).
    \end{enumerate}

    \item
    \begin{enumerate}
        \item \textbf{Montrons que $f(x) - g(x) = \dfrac{(x-3)(-x^2+3x-3)}{x-2}$ :}

        Pour $x \in D_g$, on a :
        \begin{align*}
            f(x) - g(x) &= (-x^2 + 4x - 3) - \dfrac{x-3}{x-2} \\[6pt]
                        &= \dfrac{(-x^2 + 4x - 3)(x-2) - (x-3)}{x-2}
        \end{align*}
        Calculons le numérateur :
        \begin{align*}
            (-x^2+4x-3)(x-2) - (x-3)
            &= -(x-1)(x-3)(x-2) - (x-3) \\
            &= (x-3)\big[-(x-1)(x-2) - 1\big] \\
            &= (x-3)\big[-(x^2-3x+2) - 1\big] \\
            &= (x-3)(-x^2+3x-3)
        \end{align*}
        D'où : \quad $f(x) - g(x) = \dfrac{(x-3)(-x^2+3x-3)}{x-2}$

        \item \textbf{Montrons que $-x^2+3x-3 < 0$ pour tout $x \in \mathbb{R}$ :}

        Le discriminant est $\Delta' = 3^2 - 4 \times (-1) \times (-3) = 9 - 12 = -3 < 0$.

        Puisque $\Delta' < 0$ et $a = -1 < 0$, on a $-x^2+3x-3 < 0$ pour tout réel $x$.

        \item \textbf{Point d'intersection de $(C_f)$ et $(C_g)$ :}

        $f(x) = g(x)$ équivaut à $\dfrac{(x-3)(-x^2+3x-3)}{x-2} = 0$.

        Puisque $-x^2+3x-3 < 0$ pour tout $x$ et $x \neq 2$ :
        \[ f(x) = g(x) \Leftrightarrow x - 3 = 0 \Leftrightarrow x = 3 \]

        On a $f(3) = 0$. Le point d'intersection est $\mathbf{A(3\,;\,0)}$.
    \end{enumerate}

    \item \textbf{Tracé des courbes :}

    Tracer les asymptotes $x = 2$ et $y = 1$, puis les courbes $(C_f)$ (parabole) et $(C_g)$ (hyperbole) dans le même repère, en marquant le point $A(3\,;\,0)$.

    \item \textbf{Résolution de $\dfrac{x-3}{x-2} \leq -x^2+4x-3$ :}

    L'inéquation équivaut à $g(x) \leq f(x)$, soit $f(x) - g(x) \geq 0$ :
    \[ \dfrac{(x-3)(-x^2+3x-3)}{x-2} \geq 0 \]

    Puisque $-x^2+3x-3 < 0$, cela équivaut à $\dfrac{x-3}{x-2} \leq 0$.

    \begin{center}
    \begin{tikzpicture}
        \tkzTabInit[lgt=1.5, espcl=2]{$x$ / 0.6, $x-3$ / 0.7, $x-2$ / 0.7, $\frac{x-3}{x-2}$ / 1}{$-\infty$, $2$, $3$, $+\infty$}
        \tkzTabLine{ , -, t, -, z, +, }
        \tkzTabLine{ , -, z, +, t, +, }
        \tkzTabLine{ , +, d, -, z, +, }
    \end{tikzpicture}
    \end{center}

    L'ensemble solution est : $\left]2\,;\,3\right]$

    \item \textbf{Discussion de $-x^2+4x-3-m = 0$ :}

    L'équation $f(x) = m$ représente l'intersection de $(C_f)$ avec la droite $y = m$.

    Le discriminant est $\Delta_m = b^2 - 4a(c-m) = 16 - 4(-1)(-3-m) = 16 - 4(3+m) = 4(1-m)$.
    \begin{itemize}
        \item Si $m > 1$ : $\Delta_m < 0$ : l'équation n'a \textbf{aucune solution}.
        \item Si $m = 1$ : $\Delta_m = 0$ : l'équation a \textbf{une seule solution} ($x = 2$).
        \item Si $m < 1$ : $\Delta_m > 0$ : l'équation a \textbf{deux solutions distinctes}.
    \end{itemize}
\end{enumerate}
\end{Solution}


\end{document}